# Hybrid generation prompts

User selection, 2026-09-13: combine displayed directions 1 and 3. Built-in Image Gen was used for the hybrid, nine follow-on screen boards, and three targeted correction passes. Requested dimensions are intent; actual PNG dimensions and hashes are recorded in manifest.json. Original generator files remain unchanged at their source paths.

Hybrid inputs: images/direction-01-calm.png and images/direction-03-canvas.png. Follow-on screens attached images/hybrid-01-modes.png. Each correction attached its own immediately preceding screen image. These were actual file attachments to Image Gen, not text-only descriptions.

## Hybrid anchor

```text
Create a refined production-quality desktop UI mockup combining the TWO attached design references. This is the user's chosen hybrid of direction1 and direction3, not a new set of options. Use direction1's readable, simply grouped Details panel, clear Plan versus Renovate content and restrained blue/green mode accents. Use direction3's generous canvas, compact labeled left rail and bottom taskbar. ONE paired comparison board, dimensions3072x1280, left Plan and right Renovate, same selected Kitchen, SAME geometry/camera.
Title "Plan and Renovate — one connected workspace". Small captions above two frames "Plan · Draw and adjust" and "Renovate · Plan the work". Native Obsidian-like white/off-white/light gray UI, familiar sans-serif 16px body equivalent, 44px primary actions. No branding/account/browser bezel/3D/AI. Existing plugin usability, current date2026-09-13, no dates needed.
Both frames: top "Willow House › Ground floor", centered segmented labeled switch "Plan | Renovate | Review", Undo Redo View right. Left 80px rail containing just two labeled buttons "Property" and "Layers" (these open existing property/room listing and layers). Right stable Details panel approx340px. Central flat2D floor plan: five rooms Kitchen upper right, Living room upper left, Hall middle bottom, Bedroom lower left, Bathroom lower right; charcoal walls, window/door swings, sparse neutral top-down furniture outlines, very subtle warm floor fill, NO textured wood/photo styling. Kitchen must be wider than tall with interior ratio4:3, horizontal4m and vertical3m,12m²; dimensions must be consistent with drawing. Same room plan in both frames.
PLAN: active blue mode pill; Kitchen blue outline and small accessible selection handles, external horizontal4.00m and vertical3.00m labels. Details caption PLAN, name Kitchen, Room · Ground floor,12m². Name Kitchen shown as labeled value with quiet Rename action. "Dimensions" simple rows Width4.00m Depth3.00m then primary "Change room size" button. These are resting readouts, not editable input boxes before button. Collapsed "Shape and rotation". Quiet "Renovate this room →". No renovation content inventory. Bottom floating toolbar Select Pan +Add. Footer Saved, Snap on, Zoom100%. No generic Scale not set if no reference.
RENOVATE: active green pill; Kitchen has ONLY a plain thin green outline and one small work marker inside. ABSOLUTELY NO CORNER DOTS OR GEOMETRY HANDLES on Renovate. No dimension labels anywhere in canvas. Details RENOVATE, same Kitchen/Room/Ground floor/12m² identity. Existing: Laminate floor; Planned: Oak flooring; Work(3) plain rows Remove old flooring — To do, Lay oak flooring — To do, Paint walls — To do. Green Add work primary. Below rows Materials2, Costs€2,450, Notes3; quiet Edit layout→. Bottom toolbar Select Pan +Add work More. NO geometry fields or rotation controls. Existing/planned/work are distinct.
Polish: generous whitespace, lightweight row separators, few borders, no nesting cards, coherent line icons, no hidden-only primary actions. Keep both canvases identical and content fit without clipping. In Renovate selecting information cannot manipulate geometry. Don't copy errors from refs: remove green corner handles, generic scale warning, wrong room proportions; do not introduce a third Rooms rail button.
```

## ux02-start

```text
Create a polished raster UI mockup for the EXISTING Obsidian Renovation Planner usability increment. Match the attached user-selected hybrid: clear mode distinction and readable Details from direction1, generous canvas and compact labeled navigation from direction3. Native white/off-white chrome, clear16px-like sans-serif text, good spacing, simple separators, no nested cards, primary controls44px-like. Top breadcrumb Willow House › Ground floor; always-labeled Plan/Renovate/Review switch (Plan blue, Renovate green), Undo/Redo/View right. Left rail only Property and Layers (icons AND text), stable right Details panel, floating BOTTOM taskbar. No product logo/accounts/browser bezel, 3D/AI/new domain features. Mode differs in tools and content, not only color. Geometry handles only in Plan. Current date2026-09-13; no date needed. Synthetic floor scene matches attached image as visual context, but keep drawn dimensions coherent; no numbers on a ruler unless genuinely needed. Render clean correct labels and unclipped actions; keep save state truthful. Mockup state is specified below. Target dimensions1440x1024, ONE desktop editor screen, no board title outside UI. State UX-02 empty ground floor, Plan active. Center canvas empty and restful, heading "Start your ground floor", subtext "Use what you have. You can refine the measurements later." Three simple large choices in one row: primary "Add rooms" description "Start with a room and its size"; secondary "Choose a reference plan" description "Use an image or PDF already in your vault"; quiet "Start empty" description "Draw walls or areas yourself". No duplicate Upload buttons or checklist competing in Details. Right Details shows "Ground floor" and short helpful "Your rooms and measurements will appear here." Small note "A reference plan is optional." Bottom Select Pan +Add. Footer "Saved"; no Scale not set warning, no fake room counts/costs. Main Add rooms clearly dominant. Preserve all shell positions and hierarchy of reference; do not draw any floor geometry in this empty state.
```

## ux03-room-draft

```text
Create a polished raster UI mockup for the EXISTING Obsidian Renovation Planner usability increment. Match the attached user-selected hybrid: clear mode distinction and readable Details from direction1, generous canvas and compact labeled navigation from direction3. Native white/off-white chrome, clear16px-like sans-serif text, good spacing, simple separators, no nested cards, primary controls44px-like. Top breadcrumb Willow House › Ground floor; always-labeled Plan/Renovate/Review switch (Plan blue, Renovate green), Undo/Redo/View right. Left rail only Property and Layers (icons AND text), stable right Details panel, floating BOTTOM taskbar. No product logo/accounts/browser bezel, 3D/AI/new domain features. Mode differs in tools and content, not only color. Geometry handles only in Plan. Current date2026-09-13; no date needed. Synthetic floor scene matches attached image as visual context, but keep drawn dimensions coherent; no numbers on a ruler unless genuinely needed. Render clean correct labels and unclipped actions; keep save state truthful. Mockup state is specified below. Target3072x1280 paired state board of ONE room-creation flow, not design alternatives. Small labels "Create room · valid draft" left and "Create room · correct an entry" right. Each frame full same hybrid shell with Plan active and right Details showing new-room form. Canvas has ONLY a new blue DASHED landscape rectangle4:3 proportion, no existing rooms/furniture, dimension4.00m horizontal and3.00m vertical, draft name Kitchen and12m². Left Details order heading New room, badge Draft, Name input Kitchen; optional name suggestion chips Kitchen/Living room/Bedroom; Width(m)4.00 and Depth(m)3.00; Area12m²; compact collapsed "More shape options"; unchecked "Keep adding rooms"; primary "Create room" and secondary "Cancel". Bottom task banner above bottom Select/Pan/Add toolbar: "Adding a room" and short "Adjust the size, then choose Create room." Same Create room/Cancel routes in banner can be smaller but state synchronized.
Right frame same draft but width field contains0, error border+icon and exact nearby text "Enter a width greater than 0." Keep NameKitchen and Depth3.00 untouched. Both Create room actions disabled; Cancel reachable. Preview last valid rectangle marked "Last valid preview"; do NOT render the zero as actual saved geometry. Use label "Draft — not saved" near task, footer "Existing plan saved" to distinguish draft from committed data. No unrelated shape/rotation before everyday inputs. No renovation fields. All text easily readable; no extra panels/instructions beyond current task.
```

## ux04-05-selection-precision

```text
Create a polished raster UI mockup for the EXISTING Obsidian Renovation Planner usability increment. Match the attached user-selected hybrid: clear mode distinction and readable Details from direction1, generous canvas and compact labeled navigation from direction3. Native white/off-white chrome, clear16px-like sans-serif text, good spacing, simple separators, no nested cards, primary controls44px-like. Top breadcrumb Willow House › Ground floor; always-labeled Plan/Renovate/Review switch (Plan blue, Renovate green), Undo/Redo/View right. Left rail only Property and Layers (icons AND text), stable right Details panel, floating BOTTOM taskbar. No product logo/accounts/browser bezel, 3D/AI/new domain features. Mode differs in tools and content, not only color. Geometry handles only in Plan. Current date2026-09-13; no date needed. Synthetic floor scene matches attached image as visual context, but keep drawn dimensions coherent; no numbers on a ruler unless genuinely needed. Render clean correct labels and unclipped actions; keep save state truthful. Mockup state is specified below. Target3072x1280 two-frame storyboard for selecting then editing an EXISTING room, ONE design not alternatives. Left frame "Choose the intended element" is a CONDITIONAL overlap-chooser study; right frame "Change the room size" is supported precision flow. Put small board caption "Overlap chooser: proposed usability option" outside left product screenshot.
LEFT zoomed-in Kitchen doorway, wall and adjacent Hall overlap; sensible charcoal wall/door swing. Anchor a compact context menu by doorway titled "Choose element" containing rows "Door · Kitchen", "Wall · Kitchen / Hall", "Room · Kitchen", "Room · Hall", currentDoor checked and subtly highlighted. Menu targets readable, icons distinguish kinds, no automatic modal every click. Details shows selected Door identity; bottom Select Pan +Add. Thin single blue highlight on intended door, not entire room. Name and type in all candidates.
RIGHT same shell Plan active. Kitchen Room selected. Actual interaction is explicit room size form in Details, heading "Change room size", current "Kitchen · Ground floor". Width(m)4.00, Depth(m)3.50; summary "Preview: 14 m²". Fields clearly editable; primary "Apply size", secondary "Cancel". Canvas draft outline dashed blue wider-than-tall appropriate4:3.5; previous3m edge a subdued dashed comparison; one uncluttered measurement4.00m top /3.50m side. Short honest note "This changes the room outline. Walls are unchanged." Optional small snap acquired tooltip "Aligned with room edge" near existing candidate corner, distinguish feedback from general footerSnap on. No new wall-following behavior. Status "Preview — not saved" nearform; committed planSaved elsewhere. No generic rotation handles unless needed. Highlight edit state, no unrelated renovation data, bottom mode controls stay stable.
```

## ux06-opening

```text
Create a polished raster UI mockup for the EXISTING Obsidian Renovation Planner usability increment. Match the attached user-selected hybrid: clear mode distinction and readable Details from direction1, generous canvas and compact labeled navigation from direction3. Native white/off-white chrome, clear16px-like sans-serif text, good spacing, simple separators, no nested cards, primary controls44px-like. Top breadcrumb Willow House › Ground floor; always-labeled Plan/Renovate/Review switch (Plan blue, Renovate green), Undo/Redo/View right. Left rail only Property and Layers (icons AND text), stable right Details panel, floating BOTTOM taskbar. No product logo/accounts/browser bezel, 3D/AI/new domain features. Mode differs in tools and content, not only color. Geometry handles only in Plan. Current date2026-09-13; no date needed. Synthetic floor scene matches attached image as visual context, but keep drawn dimensions coherent; no numbers on a ruler unless genuinely needed. Render clean correct labels and unclipped actions; keep save state truthful. Mockup state is specified below. For Plan mode every primary action is BLUE, never green; green primary actions belong only to Renovate. Never invent unsupported controls or fake data; use exactly the actions specified. Target1440x1024 ONE desktop screen. Plan mode active. This frame is a resting selected DOOR at the Kitchen/Hall doorway, zoomed enough to distinguish opening and host wall, both clearly visible. Blue highlight on door opening/swing, subdued host wall highlight with caption "Kitchen / Hall wall"; no room resize box. Right Details caption PLAN, heading "Door", identity "Kitchen ↔ Hall", "Ground floor". Location section rows "Host wall" value "Kitchen / Hall"; "Opening width" "0.90 m"; "Height" "2.10 m"; "Offset along wall" "0.15 m". Main BLUE "Edit opening" button leading to supported opening properties. Collapsed "Hinge and swing"; collapsed "Appearance". Quiet link "Renovate this element →". A short helpful explanation below host "The door stays attached to this wall." No Rename button, no free floating door, no new wall-relative coordinate schema. Canvas door label0.90m close to opening, avoid measurement across entire room. Bottom Select Pan +Add floating above status Saved. Keep same compact Property/Layers rail and readable stable Details panel. Do not copy right-hand Renovate controls from reference; this whole image is PLAN only.
```

## ux07-copy-paste

```text
Create a polished raster UI mockup for the EXISTING Obsidian Renovation Planner usability increment. Match the attached user-selected hybrid: clear mode distinction and readable Details from direction1, generous canvas and compact labeled navigation from direction3. Native white/off-white chrome, clear16px-like sans-serif text, good spacing, simple separators, no nested cards, primary controls44px-like. Top breadcrumb Willow House › Ground floor; always-labeled Plan/Renovate/Review switch (Plan blue, Renovate green), Undo/Redo/View right. Left rail only Property and Layers (icons AND text), stable right Details panel, floating BOTTOM taskbar. No product logo/accounts/browser bezel, 3D/AI/new domain features. Mode differs in tools and content, not only color. Geometry handles only in Plan. Current date2026-09-13; no date needed. Synthetic floor scene matches attached image as visual context, but keep drawn dimensions coherent; no numbers on a ruler unless genuinely needed. Render clean correct labels and unclipped actions; keep save state truthful. Mockup state is specified below. For Plan mode every primary action is BLUE, never green; green primary actions belong only to Renovate. Never invent unsupported controls or fake data; use exactly the actions specified. Target1440x1024, one desktop PLAN screen AFTER direct Paste into First floor. Breadcrumb Willow House › First floor. Empty spacious floor canvas contains newly pasted plain rectangular Kitchen room with four walls, one door, one window, NO furniture/work markers, selected blue. The data already exists now; this is NOT a placement ghost, no Paste here /Apply/Cancel buttons, no pre-placement wizard.
Right Details clear heading "Pasted selection", subtitle "First floor". A concise read-only scope summary with plain rows "Room 1", "Walls 4", "Door 1", "Window 1". Under separator heading "Not copied", exact short text "Work, materials, costs and evidence stay with the original." No checkbox selection options or implied configurable clipboard. Small line "Independent copy" explanation "Changes here do not change the original." Single prominent existing "Undo" action (BLUE) for recovery. Quiet navigation "View room details". Near bottom canvas a small non-blocking confirmation "Pasted into First floor" with Undo text action. Top Undo enabled, Redo subdued. Bottom toolbarSelect Pan +Add. Footer Saved. Do not show uncommitted draft labels, cross-account/system-clipboard import, duplication of financial data or furniture. Keep native calm selected hybrid shell, only2rail buttonsProperty Layers.
```

## ux08-reference

```text
Create a polished raster UI mockup for the EXISTING Obsidian Renovation Planner usability increment. Match the attached user-selected hybrid: clear mode distinction and readable Details from direction1, generous canvas and compact labeled navigation from direction3. Native white/off-white chrome, clear16px-like sans-serif text, good spacing, simple separators, no nested cards, primary controls44px-like. Top breadcrumb Willow House › Ground floor; always-labeled Plan/Renovate/Review switch (Plan blue, Renovate green), Undo/Redo/View right. Left rail only Property and Layers (icons AND text), stable right Details panel, floating BOTTOM taskbar. No product logo/accounts/browser bezel, 3D/AI/new domain features. Mode differs in tools and content, not only color. Geometry handles only in Plan. Current date2026-09-13; no date needed. Synthetic floor scene matches attached image as visual context, but keep drawn dimensions coherent; no numbers on a ruler unless genuinely needed. Render clean correct labels and unclipped actions; keep save state truthful. Mockup state is specified below. For Plan mode every primary action is BLUE, never green; green primary actions belong only to Renovate. Never invent unsupported controls or fake data; use exactly the actions specified. Target3072x1280, a THREE-stage storyboard of ONE reference-plan setup dialog, three equal wide readable panels, no decorative marketing. Title "Reference plan setup". The surrounding application is only a subtle small hint behind each modal, not a full repeated complex shell. Follow same typography and blue Plan actions as chosen hybrid.
Frame1 header "1 / 3 · Choose and prepare". Instruction "Choose an image or PDF already in your vault." Large labeled searchable picker "Choose a file" with selected Ground-floor.png, simple visible suggestions Ground-floor.png and Survey.pdf. Large image preview of neutral architectural plan. Below small options "Rotate" and collapsed "Crop and exact settings". Note "Original file stays unchanged." Bottom BLUE Continue and neutral Cancel. No path required in primary flow, no outside-vaultupload/new cloud storage. Exact path route remains within collapsedsettings.
Frame2 header "2 / 3 · Set scale". Main large same referencepreview with two BLUE endpoints A and B aligned along a printed horizontal dimension labelled4.00m; visible blue measured segment. Concise instruction "Select two points whose distance you know." At right or beneath preview show completed "First point selected", "Second point selected", then prominent labeled field "Distance between points (m)" value4.00. Collapsed "Exact point coordinates". Controls Fit image, + and − reachable. Button Choose another distance. Bottom Back, BLUE Review scale, Cancel. No pixel coordinates shown in ordinary view.
Frame3 header "3 / 3 · Review and use". Small same preview, clear summary rows "File Ground-floor.png", "Known distance 4.00 m", "Reference Locked". Opacity60% optional small slider. Important amber inline section with heading "Existing geometry will be rescaled" and plain text "Rooms, walls and planned geometry will change size to match this scale." A clearly CHECKED consent checkbox label "I understand and want to rescale the plan". Blue primary "Use reference plan", neutral Back and Cancel. This illustrates recalibrating an existing plan with consent, not deleting geometry or automatically rescaling without consent. No Save success yet, no appliedbeforeconfirmation. Keep actual whole-plan scope clear. All3stages visually coherent, unclipped no extra work/material/costfeatures.
```

## ux09-narrow

```text
Create a polished raster UI mockup for the EXISTING Obsidian Renovation Planner usability increment. Match the attached user-selected hybrid: clear mode distinction and readable Details from direction1, generous canvas and compact labeled navigation from direction3. Native white/off-white chrome, clear16px-like sans-serif text, good spacing, simple separators, no nested cards, primary controls44px-like. Top breadcrumb Willow House › Ground floor; always-labeled Plan/Renovate/Review switch (Plan blue, Renovate green), Undo/Redo/View right. Left rail only Property and Layers (icons AND text), stable right Details panel, floating BOTTOM taskbar. No product logo/accounts/browser bezel, 3D/AI/new domain features. Mode differs in tools and content, not only color. Geometry handles only in Plan. Current date2026-09-13; no date needed. Synthetic floor scene matches attached image as visual context, but keep drawn dimensions coherent; no numbers on a ruler unless genuinely needed. Render clean correct labels and unclipped actions; keep save state truthful. Mockup state is specified below. For Plan mode every primary action is BLUE, never green; green primary actions belong only to Renovate. Never invent unsupported controls or fake data; use exactly the actions specified. Target1600x1024 board with THREE narrow desktop editor panes, each460px wide and about900px high, aligned side by side. These are split-pane states of the SAME design, NOT phone screens: NO device bezel/notch/OSbars. Small external captions "Plan · canvas", "Plan · room draft", "Renovate · Details".
All three: breadcrumb Willow House › Ground floor top; below always-visible labeled Plan Renovate Review controls; another compact row Undo/View/Details if needed. Do not clip text. Compact Property and Layers navigation can appear as two labeled rail entries or a single clearly labeled collapsible group. No full side-by-side inspector column at460px.
LEFT Plan active BLUE, canvas visible with selected Kitchen and relevant small dimension labels, bottomfloating Select Pan +Add, visible button "Kitchen details". View menu includes recognizable Fit route (may show "Fit plan" besideView). Selected identity still clear; no obligatory horizontal scrolling in forms. No dialog.
MIDDLE Plan active BLUE, Details drawer open with heading "New room" badgeDraft and button "Back to canvas". It covers canvas intentionally. NameKitchen field; Width(m)4.00; Depth(m)3.00; Area12m²; collapsedMore shape options; Keep adding rooms unchecked. Sticky visible footer BLUECreate room and Cancel. Other fields can scroll vertically, footer never hidden; text no less than14px. Draft text "Not saved yet" nearCreate controls. Closing drawer retains these inputs (illustrated by a concise external caption "Return without losing the draft", not marketing inside app).
RIGHT Renovate active GREEN, Detailsdrawer open titleKitchen /12m² /Ground floor and Back to canvas. ExistingLaminate floor, PlannedOak flooring, Work3 with simple short rows, primarygreenAdd work. Materials2 Costs€2,450 Notes3 as lower links. Quiet Edit layout→. NO dimensions, corner handles or rotation fields. Compact bottom shared navigation stable. FooterSaved only committed state.
Show mode difference through labeled switch, active accent, content and actions. Main controls44px-like; avoid mixing desktop fullwidth layout into these panels. Maintain approachable native typography and calm white surfaces.
```

## ux10-dark

```text
Create a polished raster UI mockup for the EXISTING Obsidian Renovation Planner usability increment. Match the attached user-selected hybrid: clear mode distinction and readable Details from direction1, generous canvas and compact labeled navigation from direction3. Native white/off-white chrome, clear16px-like sans-serif text, good spacing, simple separators, no nested cards, primary controls44px-like. Top breadcrumb Willow House › Ground floor; always-labeled Plan/Renovate/Review switch (Plan blue, Renovate green), Undo/Redo/View right. Left rail only Property and Layers (icons AND text), stable right Details panel, floating BOTTOM taskbar. No product logo/accounts/browser bezel, 3D/AI/new domain features. Mode differs in tools and content, not only color. Geometry handles only in Plan. Current date2026-09-13; no date needed. Synthetic floor scene matches attached image as visual context, but keep drawn dimensions coherent; no numbers on a ruler unless genuinely needed. Render clean correct labels and unclipped actions; keep save state truthful. Mockup state is specified below. For Plan mode every primary action is BLUE, never green; green primary actions belong only to Renovate. Never invent unsupported controls or fake data; use exactly the actions specified. OVERRIDE light appearance: create DARK-THEME counterpart of attached paired Plan/Renovate screen. Target3072x1280, two equal frames same layout, same Kitchen/camera. Charcoal native Obsidian surfaces, nearwhite readable text, subtle gray separators, visible focus, medium blue activePlan and medium emerald activeRenovate with legible labels. No black-on-black dimensions. Do NOT darken by a uniform translucent overlay; recompose proper dark UI token roles. Plan primary actions blue; Renovate primary actions green. No gradients or neon glow.
Left Plan: compact labeledProperty/Layers rail, fullsamefloorplan on neutral dark canvas with pale warm room fills, Kitchen selectedblue outline/handles and legible4.00m/3.00m external labels. RightDetailscaptionPLAN, Kitchen Room Groundfloor12m², resting rowsWidth4.00mDepth3.00m, BLUEChange room size, collapsedShape and rotation, quietRenovate this room→. Bottom SelectPan+Add. FooterSaved; no genericScale not set.
RightRenovate: identical plan and framing, Kitchen plain emerald outline NO CORNER DOTS/HANDLES/resize/dimensions. Work marker inside. RightDetailscaptionRENOVATE, sameKitchen12m², ExistingLaminatefloor, PlannedOakflooring, Work3 simple rows allTo do, greenAddwork, Materials2 Costs€2,450 Notes3 Editlayout→. BottomSelectPan+AddworkMore. FooterSaved. Mode text/icons make distinction work withoutcolor.
Title small outside frames "The same workspace in dark appearance". No changes to geometry, data, toolsets or keyboardroutes just for theme.
```

## ux11-12-recovery-mode-guard

```text
Create a polished raster UI mockup for the EXISTING Obsidian Renovation Planner usability increment. Match the attached user-selected hybrid: clear mode distinction and readable Details from direction1, generous canvas and compact labeled navigation from direction3. Native white/off-white chrome, clear16px-like sans-serif text, good spacing, simple separators, no nested cards, primary controls44px-like. Top breadcrumb Willow House › Ground floor; always-labeled Plan/Renovate/Review switch (Plan blue, Renovate green), Undo/Redo/View right. Left rail only Property and Layers (icons AND text), stable right Details panel, floating BOTTOM taskbar. No product logo/accounts/browser bezel, 3D/AI/new domain features. Mode differs in tools and content, not only color. Geometry handles only in Plan. Current date2026-09-13; no date needed. Synthetic floor scene matches attached image as visual context, but keep drawn dimensions coherent; no numbers on a ruler unless genuinely needed. Render clean correct labels and unclipped actions; keep save state truthful. Mockup state is specified below. For Plan mode every primary action is BLUE, never green; green primary actions belong only to Renovate. Never invent unsupported controls or fake data; use exactly the actions specified. Target3072x1280 three-panel storyboard of safety states using the same selected hybrid shell. Each pane emphasizes a readable message and next action, surrounding plan can be subdued. Three captions outside product UI "Saved, view needs refresh", "Save not confirmed", "Switching with a draft".
Frame1: Plan active, retainedKitchen scene. Persistent amber notice belowheader "Your change was saved, but this view needs refreshing." Text "Read the saved plan again to continue." BLUEbutton "Refresh view". Editing controls disabled because projection stale, selectedidentity stays. Footer EXACT "Saved · View out of date", never an unqualified Saved. No repeated Save button.
Frame2: Plan active, same selectedKitchen scene and size-edit form remains with values4.00/3.50, Draftkept. Persistent serious message "The save could not be confirmed." Text "Your draft is kept. Check the saved plan before trying this change again." BLUE action "Read saved plan"; secondary "Open source note". Apply paused/disabled. Footer "Save not confirmed". No automatic retry, no greenSaved or success check. Failure state does not erase input.
Frame3: Plan active behind centered confirmation dialog and uncommitted new-roomdraft. User attempted Renovate but MODE IS STILLPLAN until decision. Dialog heading "Leave this room draft?" body "This room has not been created. Switching to Renovate will discard the draft." PrimaryBLUE "Keep editing"; secondary neutral "Discard draft and switch". NO SaveandSwitch invented action. No activecommit/busywrite being cancelled. Underlying Sizefields remain, diagramdashedDraft. Footer existingplanSaved may be qualified "Existing plan saved". The dialog explains draft only; committedgeometry neverdeleted. Never render Renovate mode as active before confirming.
Unclipped controls, exact text, no giant errorillustrations or invented workflow features. These are interaction contracts not generic browseralerts.
```

## ux04-05-selection-precision-correction

```text
Edit this attached UI mockup only to correct the precision controls. Preserve its overall two-screen composition, size, typography, floor scene, mode switch, rail, panels and overlap menu. In the RIGHT Plan frame change the "Apply size" primary button from green to BLUE matching active Plan. Correct the vertical blue dimension label to exactly "3.50 m" (readable normal digits); horizontal label stays4.00m, area14m², widthfield4.00, depthfield3.50. In the LEFT Door Details remove the unsupported Rename action and replace with the same BLUE "Edit opening" action used for opening properties. Do not change the chooser candidates. Keep text "This changes the room outline. Walls are unchanged." Clear footer qualification on right "Preview — not saved" beside field already remains. Do not add, remove or alter other content. Output same image dimensions as input.
```

## ux11-12-recovery-mode-guard-correction

```text
Correct only inconsistent geometry numbers in this three-frame UI storyboard. Preserve every safety message, button, layout, mode state and frame composition. In ALL frames the Kitchen is 4.00 m wide by 3.00 m deep, with area12 m². Therefore change every "3.50 m" depth value and vertical dimension label to "3.00 m", preserving width4.00m and area12m². All three frames need coherent quantities. In the middle frame add short "Draft retained" text below the save-warning message without hiding any existing controls. Do not alter the saved-but-stale versus save-not-confirmed semantics; no greenSaved in middle. In the last frame keep Plan active and Keep editing /Discard draft and switch exactly. Output same dimensions as attached.
```

## ux09-narrow-correction

```text
Edit ONLY the MIDDLE pane's bottom save-state label in this three-pane narrow editor mockup. It is an uncommitted New room draft, so replace the middle bottom green check and "Saved" with a neutral draft icon and exact text "Draft not saved". Keep the middle "Not saved yet" text and Create room/Cancel actions. Left and right panes still show Saved. Preserve all three layouts, sizes, typography, mode colors, fields, content and images exactly. Do not add new controls or change any other labels. Output same dimensions.
```
