# I01 — Make the active mode unmistakable

- Packet and outcome: Added a visible current-mode label and reserved bilingual caption beside the existing Plan/Renovate/Review radio group. Active tabs now carry a semantic accent, border/underline cue and stronger weight; radio accessible names include the caption.
- Assigned model/reasoning: `gpt-5.6-luna` / high.
- Status: review; PR is open and stacked on the reviewed I00 branch.
- Prerequisite PRs and confirmed merge SHAs: I00 PR #174 (`codex/usability-i00-contracts`) is the reviewed stack root at `50e6a8dd30658d38e8f7474723fcedb00182a937`; it remains unmerged, and this branch consumes its reserved caption contracts through stack ancestry as authorized by the session prompt.
- Base SHA: `50e6a8dd30658d38e8f7474723fcedb00182a937`.
- Branch/worktree: `codex/usability-i01-mode-header` / `D:\Projects\renovation-planner\.worktrees\usability-i01-mode-header`.
- PR URL and current commit: pending push/open at receipt write time; report the final URL and SHA in the handoff.
- Owned files changed; any explicit ownership transfer: `src/presentation/editor/shell/EditorContextBar.vue`, `styles/editor-visual-shell.css`, `tests/presentation/editor/usability/i01-mode-header.test.ts`, and this receipt. No ownership transfer.
- Shared-file integration request and resolution: none. Consumed I00's exact `editor.perspective.plan.caption`, `editor.perspective.renovate.caption` and `editor.perspective.review.caption` keys; no root, locale, CSS-index or action changes were needed.
- Commands run and exact results (include no-match/skipped limits): `npm run check:fast -- tests/presentation/editor/usability/i01-mode-header.test.ts` passed: 1 matched test file, 1 test. Mandated `npm run check:fast -- tests/presentation/editor/renovationRoutes.test.ts tests/presentation/editor/shellFidelity.test.ts` passed: 2 matched test files, 21 tests. `npx eslint src/presentation/editor/shell/EditorContextBar.vue tests/presentation/editor/usability/i01-mode-header.test.ts` passed with 0 errors and 0 warnings. `git diff --check` passed. The full `npm run check` and CI remain I16/integration-owned.
- Browser evidence paths, fixture/URL, viewport, theme, locale and input method: inspected the connected production-equivalent Vite harness at `http://127.0.0.1:5173/?view=plan-editor&reference&planning&fidelity` and its light variant with `theme=light`, using the default Ground floor fixture at the observed 1280×720 viewport. Exercised Plan → Renovate and Review by real browser clicks in light and dark appearances; inspected German light copy at `...?view=plan-editor&reference&planning&fidelity&theme=light&lang=de`. Focused Plan's real radio and pressed `ArrowRight`; AX focus moved to Renovate and announced `Renovate: Plan the work`. The named `hybrid-01-modes.png` and `ux10-dark.png` were inspected as hierarchy guidance only, not treated as runtime evidence. Narrow viewport capture was not performed because the connected in-app browser exposed no viewport override and browser zoom did not change the responsive viewport.
- Native/AT/user-study checks actually performed: none; the harness AX tree was inspected, but this is not native Obsidian, a screen-reader certification, or participant evidence.
- Unperformed checks and why: 460px/narrow visual capture, native Obsidian, custom community theme, screen-reader behavior and novice study remain unperformed. The harness's default route rendered the Ground floor setup state rather than the artwork's populated room scene; this packet only changed the always-mounted editor header and mode controls.
- Preserved command/history/geometry/state boundaries: retained `[data-rp-perspective]` selectors, role/radio semantics, roving `tabindex`, keyboard Home/End/arrow switching, `runtime.renovation.perspective`, breadcrumb order, stable history hooks and Review's hidden-but-space-reserving history block. No persistence, write, command, selection, camera, draft guard or Review snapshot logic changed.
- Remaining risks, conditional decisions and release impact: Full build/lint/coverage/fallow and CI are still required before merge. U4 overlap chooser and arbitrary-corner non-drag access remain conditional-deferred under I00; this packet does not claim an accessibility pass. Narrow and native visual/AT evidence should be collected by the later verification/integration packets.
- Reviewer findings, follow-up SHA and addressed threads: none.
- Confirmed merge SHA and cleanup result: pending review/merge; do not clean this worktree or branch until merge is confirmed.
