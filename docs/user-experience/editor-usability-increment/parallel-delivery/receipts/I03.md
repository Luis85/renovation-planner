# I03 — Put common Room edits first

- Packet and outcome: Plan Room details now lead with the existing common name/size actions, advanced shape/rotation/lock controls sit behind the existing More actions disclosure, and Plan exposes one Renovate hand-off instead of the full Renovation inventory.
- Assigned model/reasoning: `gpt-5.6-luna` / high.
- Status: implementing; provisional commit awaiting the corrected I04 parent before final verification and PR creation.
- Prerequisite PRs and confirmed merge SHAs: I00 and I01 are explicitly dispositioned through the reviewed stack ancestry. The reviewed I04 tip supplied by the coordinator is `ded93356f8820fa6776cbe790ec955e2b4c781ce`.
- Base SHA: `ded93356f8820fa6776cbe790ec955e2b4c781ce` (`codex/usability-i04-renovate-details`).
- Branch/worktree: `codex/usability-i03-room-details` / `D:\Projects\renovation-planner\.worktrees\usability-i03-room-details`.
- PR URL and current commit: no PR yet by coordinator instruction; provisional commit will be recorded after commit/push.
- Owned files changed; any explicit ownership transfer: `src/presentation/editor/shell/RoomInspector.vue`, `src/presentation/editor/shell/SpatialInspectorActions.vue`, `styles/editor-inspector.css`, this receipt, and new `tests/presentation/editor/usability/i03-room-details.test.ts`. No ownership transfer.
- Shared-file integration request and resolution: none. Existing localized strings are reused (`editor.rename.action`, `editor.resize.action`, `editor.resize.unsupported`, `editor.structure.more`, `renovation.renovate`); no locale, root composition or CSS-index edit was needed.
- Commands run and exact results (include no-match/skipped limits): `npx vitest run tests/presentation/editor/usability/i03-room-details.test.ts` passed: 1 file, 3 tests (the `it.each` covers irregular and rotated outlines). The mandated `npm run check:fast -- tests/presentation/editor/shell/roomInspector.test.ts tests/presentation/editor/rotationInspectorRoutes.test.ts` was run once and stopped in `vue-tsc` with the three known pre-existing stacked I04 errors in `src/presentation/editor/renovation/RenovationEntry.vue` lines 23 (possibly undefined `structure`); it did not reach Vitest. `npx oxlint --deny-warnings` over the two owned Vue files and new test passed. Targeted `npx eslint` over the owned Vue files, CSS and new test passed with one warning that `styles/editor-inspector.css` is ignored because no matching ESLint configuration was supplied. `git diff --check` passed with no output. No zero-match test run was used.
- Browser evidence paths, fixture/URL, viewport, theme, locale and input method: none claimed; no connected production-equivalent harness evidence was available in this session. No component specimen or alternate browser was substituted.
- Native/AT/user-study checks actually performed: none.
- Unperformed checks and why: final connected-harness ordinary/irregular/locked/mode routing, dark/narrow/German rendering, pointer/focus behavior in the real harness, native Obsidian, AT and participant checks remain unperformed. Final focused and mandated gates must run after the coordinator supplies the corrected I04 parent.
- Preserved command/history/geometry/state boundaries: common actions still call the existing rename, eligible axis-aligned resize and area-details routes; irregular/rotated rooms continue to show the existing unsupported-size message. The new Renovate button calls only `runtime.renovation.focus(zoneId, 'overview')`, performs no write/history entry, and leaves Renovate's existing inventory to I04. Existing rotation, curve, lock, selection, no-selection/multi-selection, locked, area and Delete behavior remain routed through their existing components and commands. No geometry/persistence/schema/domain change was made.
- Remaining risks, conditional decisions and release impact: the provisional branch cannot claim a full check pass while the stacked I04 parent has the known type errors. The route label reuses the existing localized `Renovate` string with the selected name; no new locale key was introduced. I00's deferred U4 chooser and non-drag arbitrary-corner limitation remain unchanged and are not an I03 accessibility pass.
- Reviewer findings, follow-up SHA and addressed threads: none for I03; coordinator has identified the I04 parent type errors and instructed this branch not to edit or gate against them.
- Confirmed merge SHA and cleanup result: pending; do not clean this worktree.
