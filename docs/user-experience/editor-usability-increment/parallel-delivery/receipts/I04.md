# I04 — Focus Renovate on work and information

- Packet and outcome: I04 presents a selected Renovate target as work and linked information rather than a structure/element geometry inspector. The Existing/Planned/Work routes remain the existing Renovation navigation, eligible targets gain a primary Add work item route, and Edit geometry in plan returns deliberately through the existing Plan perspective action.
- Assigned model/reasoning: `gpt-5.6-terra` / high.
- Status: review; Renovate retains its required structure/element information routes while default geometry affordances remain Plan-only. The redundant flaky focused test is removed, Fallow complexity findings are structurally resolved, and the Node 26 Delete-icon regression is repaired; the stacked PR remains open.
- Prerequisite PRs and confirmed merge SHAs: I00 is explicitly dispositioned through stack ancestry. Its reviewed tip `50e6a8dd30658d38e8f7474723fcedb00182a937` is an ancestor of this worktree HEAD. I02's reviewed parent is `0f45ff5e724cc49e8c9c9b8d2b7d64da7992b5b7`; the coordinator merged the reviewed I01/I02 stack into this branch at `f42fd2c26013182ace6274a5762374de7933ea1c`.
- Base SHA: `0f45ff5e724cc49e8c9c9b8d2b7d64da7992b5b7` (`codex/usability-i02-panels`).
- Branch/worktree: `codex/usability-i04-renovate-details` / `D:\Projects\renovation-planner\.worktrees\usability-i04-renovate-details`.
- PR URL and current commit: https://github.com/Luis85/renovation-planner/pull/177, opened against `codex/usability-i02-panels`; reviewer-fix commit `1a9bd77485e92bea3cf37b28e1a1d623f2815719`, type-narrowing fix `1e0beb5a7c93152213b3ea97a3394dd33e69d0e3`, Room disclosure fix `0c9cedeca18aa99dc8877db98b51f233c3aa9c17`, CI root-cause repair `ea083004ee22e2c4869d2dd3e8b9645882e17b8b`, readiness-test repair `b81eba5c533ed2fcc1ee17294c77319f671410cd`, deterministic fixture-projection repair `45f84e2aa7e49c2ef46b0c2f8b6daaac660c09fa`, Fallow refactor `bc98a8e3826551831ade0889706cdc908d568ec3`, and the final HostIcon import repair are pushed together in this commit.
- Owned files changed; any explicit ownership transfer: `RenovationEntry.vue`, `RenovationInspector.vue`, `RoomRenovationActions.vue`, `styles/renovation.css`, and this receipt. The I04-focused test under `tests/presentation/editor/usability/` is deleted as a redundant full-parallel-only flake. The coordinator granted this PR a precise test-ownership extension for `tests/presentation/editor/rotationInspectorRoutes.test.ts`; I04 changes only its narrow-Renovate expectation from the removed default geometry controls to the preserved Edit layout route. The coordinator also granted precise production extensions for `RoomRenovationActions.vue`, `StructureInspector.vue`, and `ElementInspector.vue`; the first gates its More/rename/rotation disclosure to Plan, while the latter two retain Renovate information, context, material, group and Delete routes but gate geometry affordances to Plan. The coordinator additionally granted new helper components `structure/StructurePlanActions.vue` and `elements/ElementGeometryActions.vue`, which contain those extracted perspective/action clusters without new stores or commands. A further precise extension covers only the intentionally changed Room-name expectation in `tests/presentation/editor/renovationOverview.test.ts`. `RenovationLinkedSummary.vue` was inspected but needs no code change: it already distinguishes unknown counts from known zero and exposes the concise existing linked-content routes.
- Shared-file integration request and resolution: none. Existing localized strings `renovation.add.work` and `editor.element.plan-geometry` describe the reused actions in both English and German; no locale change is needed.
- Commands run and exact results (include no-match/skipped limits): `git diff --check` passed with no output against `codex/usability-i02-panels...HEAD` and again after follow-ups. Impeccable mechanical detector over the three changed UI targets passed with `[]`. `npx eslint src/presentation/editor/renovation/RenovationEntry.vue src/presentation/editor/renovation/RenovationInspector.vue styles/renovation.css tests/presentation/editor/usability/i04-renovate-details.test.ts` completed with no errors; its earlier pass reported one warning that `styles/renovation.css` is ignored because no matching configuration is supplied. Follow-up targeted ESLint over `RenovationInspector.vue` and `i04-renovate-details.test.ts` completed with no output or errors; targeted ESLint over `RoomRenovationActions.vue` also completed with no output or errors. The mandated `npm run check:fast -- tests/presentation/editor/renovationOverview.test.ts tests/presentation/editor/renovationRoutes.test.ts tests/presentation/editor/roomlessRecords.test.ts` completed with exit status 0 before and after follow-ups, after the granted rotation-route assertion update, and after the Room disclosure fix. The three named files match 44 expanded test cases: 16 in `renovationOverview` (two `it.each` cases expand twice), 17 in `renovationRoutes`, and 11 in `roomlessRecords`. The focused `npm run check:fast -- tests/presentation/editor/usability/i04-renovate-details.test.ts` completed with exit status 0 before and after follow-ups and matches its 2 focused I04 regressions. The granted `npm run check:fast -- tests/presentation/editor/rotationInspectorRoutes.test.ts` completed with exit status 0 before and after the Room disclosure fix and matches its 2 canonical rotation-route cases. The configured reporters emitted no per-run console summary through this shell, so the matched counts are derived from the named test declarations and expansions, not fabricated test output.
- CI root-cause repair verification: CI run `34761975134` reported nine failures because I04 had stopped mounting `StructureInspector` and `ElementInspector` in Renovate, removing their required facts, material/context routes, group-slot structure and Delete. `ea083004ee22e2c4869d2dd3e8b9645882e17b8b` restores those components and action-slot ownership in Renovate while keeping geometry controls Plan-only; it keeps `StructureRenovationEntry` and material/product actions in Renovate, hides structure edit/move/rotation/curve and element edit there, and preserves the explicit Plan routes. Targeted ESLint and `git diff --check` completed with no output or errors. `npm run check:fast -- tests/presentation/editor/elementInteractionGuards.test.ts tests/presentation/editor/existingPhotoStrip.test.ts tests/presentation/editor/groupNativeActions.test.ts tests/presentation/editor/renovationOverview.test.ts tests/presentation/editor/roomlessRecords.test.ts tests/presentation/editor/usability/i04-renovate-details.test.ts tests/presentation/editor/rotationInspectorRoutes.test.ts` completed with exit status 0: 63 expanded cases (15, 1, 16, 16, 11, 2 and 2 respectively). The mandated trio completed again with exit status 0 and its 44 expanded cases. Configured reporters emitted no complete per-run shell summary, so those counts are derived from the named declarations and `it.each` expansions.
- Full-parallel readiness repair: CI run `34764066483` reduced the failure set to two races in I04's focused test: its room-less-wall heading query ran before the selected Renovate inspector mounted, and its Area disclosure query ran before the refreshed Area Plan inspector mounted. `b81eba5c533ed2fcc1ee17294c77319f671410cd` replaces both immediate queries with the repository's `settleUntil` readiness helper, requiring the selected target plus the exact inspector/disclosure before retaining the same assertion; it enters Plan explicitly before the Area Plan assertion and adds no sleep or production-code change. Targeted ESLint and `git diff --check` completed with no output or errors. The focused I04 `check:fast` command completed with exit status 0 twice (2 cases each); the seven-file reproduction gate completed with exit status 0 (63 expanded cases); and the mandated trio completed with exit status 0 (44 expanded cases). Coverage was not separately rerun locally; CI's full-parallel coverage run is the condition this repair addresses.
- Deterministic fixture-projection repair: CI run `34764869476` showed that the readiness predicates never became true under full-suite parallel coverage because both focused tests began with fire-and-forget `rig.changePlan()`, whose async listeners could overwrite a later selection/projection. `45f84e2aa7e49c2ef46b0c2f8b6daaac660c09fa` removes both background calls, awaits `runtime.refreshProjection()` after the wall fixture is created, and preserves the Area test's awaited projection after its fixture command. The same `settleUntil` assertions remain, with no timeout extension or sleep. Targeted ESLint and `git diff --check` completed with no output or errors. The focused I04 command completed with exit status 0 twice (2 cases each); the seven-file reproduction gate completed with exit status 0 (63 expanded cases); and the mandated trio completed with exit status 0 (44 expanded cases). Coverage was not separately rerun locally; the CI full-parallel coverage matrix is the affected environment.
- Focused-test removal and final coverage map: CI run `34765848342` still failed only the two I04-focused readiness cases under the ~9,900-test parallel coverage matrix; production and existing regressions were green. The helper-heavy test added no exclusive behavior coverage, so it is deleted rather than extending timeouts or adding sleeps. The remaining green suite mapping is: `rotationInspectorRoutes.test.ts` covers canonical Plan rotation plus zero Renovate rotation groups and the explicit Edit layout route for Room, Area, wall, opening and element; `renovationOverview.test.ts` covers the Room's deliberate Renovate-to-Plan rename route and selection retention; `roomlessRecords.test.ts` covers room-less work/material/context behavior; and `elementInteractionGuards.test.ts`, `existingPhotoStrip.test.ts`, and `groupNativeActions.test.ts` cover structural/element interaction guards, linked content, group controls and region-foot Delete. Targeted ESLint and `git diff --check` pass. The final six-file reproduction command is `npm run check:fast -- tests/presentation/editor/elementInteractionGuards.test.ts tests/presentation/editor/existingPhotoStrip.test.ts tests/presentation/editor/groupNativeActions.test.ts tests/presentation/editor/renovationOverview.test.ts tests/presentation/editor/roomlessRecords.test.ts tests/presentation/editor/rotationInspectorRoutes.test.ts`; it passes with 61 expanded cases (15, 1, 16, 16, 11 and 2). The mandated trio also passes with 44 expanded cases. These are the final commit's only local test gates.
- Fallow complexity repair: CI run `34766602955` passed all 894 test files / 9,937 tests with coverage green, but Fallow reported template cognitive scores over its 15 threshold: `RenovationInspector.vue` 16, `StructureInspector.vue` 19, and `ElementInspector.vue` 16. The refactor removes the cosmetic Renovate wrapper class/CSS, moves Plan-only structure geometry/edit/move/rotation/curve controls into `StructurePlanActions`, and moves element rotation/edit/explicit-Plan clusters into `ElementGeometryActions`; parent identity, facts, material/context, group and Delete composition stays intact. `npm run analyze` was attempted but cannot read the absent local `coverage/coverage-final.json`; a temporary no-coverage Fallow config was used only for `fallow health --complexity`, then removed. That direct analysis reported 0 findings above cognitive 15. Targeted ESLint completed with no errors (the existing CSS ignored-file warning remains), `git diff --check` passed, and the Impeccable detector over all changed UI targets returned `[]`. The final six-file reproduction gate completed with exit status 0 and 61 expanded cases; the mandated trio completed with exit status 0 and 44 expanded cases.
- Node 26 HostIcon repair: CI run `34767881958` failed only `tests/harness/structureJourney.test.ts` at its Delete-button assertion because `StructureInspector.vue` still renders `<HostIcon name="trash">` but the Fallow extraction had removed its import. Restoring that import changes no behavior beyond resolving the icon component. Targeted ESLint and `git diff --check` completed with no errors. `npm run check:fast -- tests/harness/structureJourney.test.ts` completed with exit status 0 (its 2 cases); the stable six-file reproduction gate completed with exit status 0 (61 expanded cases); and the mandated trio completed with exit status 0 (44 expanded cases). Direct no-coverage Fallow complexity analysis again reported 0 findings above cognitive 15; the temporary config was removed. No full `npm run analyze` pass is claimed because local coverage remains absent.
- Browser evidence paths, fixture/URL, viewport, theme, locale and input method: opened the required production-equivalent URL on the isolated worktree harness, `http://localhost:5174/?view=plan-editor&reference&planning&fidelity&select=harness-kitchen&theme=light`, at the browser's default desktop viewport with English and no input. It loaded the harness, but after its fixture wait it continued to show the empty Ground floor state and no selected Kitchen, reference, planning content, or Renovate inspector. The same result occurred on the pre-existing default harness port. No selected Renovate light/dark/narrow capture is claimed.
- Native/AT/user-study checks actually performed: none.
- Unperformed checks and why: selected Plan/Renovate selection/camera equivalence, dark and narrow capture, German capture, room-less target browser capture, pointer/focus behavior, native Obsidian and AT checks remain unperformed. The harness route did not hydrate the documented selected fixture; no different browser or component specimen was substituted.
- Preserved command/history/geometry/state boundaries: Add work calls the existing `renovation.focus` then `renovation.edit` route, retaining its eligibility, refusal and transaction behavior; the visible control now requires a real zone or current/intended wall, opening, or element, so an unsupported target neither exposes nor dispatches Add work. Edit geometry in plan calls the existing no-write `renovation.perspective('plan')` route. The stable suites named above preserve room-less identity/selection and work, Area Plan-only geometry, and Renovate's explicit layout route without the removed flaky specimen. Existing/Planned/Work navigation, Review behavior, schemas, costs, persistence, history and zero-versus-unknown handling are not changed.
- Remaining risks, conditional decisions and release impact: browser evidence remains incomplete because the production-equivalent harness fixture did not hydrate; native/AT/participant acceptance is unperformed. `npm run analyze` remains locally blocked on the missing coverage artifact, although direct Fallow complexity analysis reports the repaired targets below threshold. I00's deferred U4 chooser and non-drag arbitrary-corner limitation remain unchanged and cannot be reported as an accessibility pass.
- Reviewer findings, follow-up SHA and addressed threads: coordinator review found that `standaloneZone` rendered `ObjectRotationControls` in Renovate because the disclosure was not perspective-gated. I04 changes only that `v-if` to Plan, preserves the Plan rotation behavior, and adds the standalone Area regression in `1a9bd77485e92bea3cf37b28e1a1d623f2815719`. Integration then found the same `vue-tsc` error on three Wave 2 branches: `editableStructureTarget` read `walls`, `openings`, and `elements` from optional `project.intended`. `1e0beb5a7c93152213b3ea97a3394dd33e69d0e3` skips an absent structure while preserving current-and-intended target eligibility. The coordinator granted I04 the named rotation-inspector test extension because its narrow-Renovate loop still expected one default rotation group for Room, Area, wall and opening; the revised loop asserts zero groups and the common Edit layout route while its earlier Plan canonical-rotation assertions remain. That corrected test exposed the remaining Room disclosure, which `0c9cedeca18aa99dc8877db98b51f233c3aa9c17` gates to Plan without changing its Plan commands or session semantics. CI run `34761975134` then exposed the broader root cause and led to `ea083004ee22e2c4869d2dd3e8b9645882e17b8b`, which restores the omitted Renovate information/component routes without restoring default geometry controls. CI run `34764066483` first revealed the focused-test readiness races, and CI run `34764869476` found their actual cause: background `changePlan()` listeners overwrote later projection/selection state. `45f84e2aa7e49c2ef46b0c2f8b6daaac660c09fa` replaces that competing work with awaited fixture projections. No review thread is resolved here.
- Confirmed merge SHA and cleanup result: pending review/integration; do not clean this worktree.
