# I04 — Focus Renovate on work and information

- Packet and outcome: I04 presents a selected Renovate target as work and linked information rather than a structure/element geometry inspector. The Existing/Planned/Work routes remain the existing Renovation navigation, eligible targets gain a primary Add work item route, and Edit geometry in plan returns deliberately through the existing Plan perspective action.
- Assigned model/reasoning: `gpt-5.6-terra` / high.
- Status: review; provisional commit pushed, awaiting stack integration and the coordinator's shared heavy-verification slot.
- Prerequisite PRs and confirmed merge SHAs: I00 is explicitly dispositioned through stack ancestry. Its reviewed tip `50e6a8dd30658d38e8f7474723fcedb00182a937` is an ancestor of this worktree HEAD.
- Base SHA: `50e6a8dd30658d38e8f7474723fcedb00182a937`.
- Branch/worktree: `codex/usability-i04-renovate-details` / `D:\Projects\renovation-planner\.worktrees\usability-i04-renovate-details`.
- PR URL and current commit: no PR yet; provisional commit SHA is recorded after push.
- Owned files changed; any explicit ownership transfer: `RenovationEntry.vue`, `RenovationInspector.vue`, `styles/renovation.css`, this receipt, and new `tests/presentation/editor/usability/i04-renovate-details.test.ts`. `RenovationLinkedSummary.vue` was inspected but needs no code change: it already distinguishes unknown counts from known zero and exposes the concise existing linked-content routes.
- Shared-file integration request and resolution: none. Existing localized strings `renovation.add.work` and `editor.element.plan-geometry` describe the reused actions in both English and German; no locale change is needed.
- Commands run and exact results (include no-match/skipped limits): `git diff --check` passed with no output. Impeccable mechanical detector over the three changed UI targets passed with `[]`. `npx eslint src/presentation/editor/renovation/RenovationEntry.vue src/presentation/editor/renovation/RenovationInspector.vue styles/renovation.css tests/presentation/editor/usability/i04-renovate-details.test.ts` completed with no errors; ESLint reported one warning that `styles/renovation.css` is ignored because no matching configuration is supplied. The required `npm run check:fast -- tests/presentation/editor/renovationOverview.test.ts tests/presentation/editor/renovationRoutes.test.ts tests/presentation/editor/roomlessRecords.test.ts` is deliberately not run: I02 owns the shared slot until the coordinator grants it. The I04 regression is also pending that slot.
- Browser evidence paths, fixture/URL, viewport, theme, locale and input method: opened the required production-equivalent URL on the isolated worktree harness, `http://localhost:5174/?view=plan-editor&reference&planning&fidelity&select=harness-kitchen&theme=light`, at the browser's default desktop viewport with English and no input. It loaded the harness, but after its fixture wait it continued to show the empty Ground floor state and no selected Kitchen, reference, planning content, or Renovate inspector. The same result occurred on the pre-existing default harness port. No selected Renovate light/dark/narrow capture is claimed.
- Native/AT/user-study checks actually performed: none.
- Unperformed checks and why: selected Plan/Renovate selection/camera equivalence, dark and narrow capture, German capture, room-less target browser capture, pointer/focus behavior, native Obsidian and AT checks remain unperformed. The harness route did not hydrate the documented selected fixture; the heavy test gate is queued behind I01.
- Preserved command/history/geometry/state boundaries: Add work calls the existing `renovation.focus` then `renovation.edit` route, retaining its eligibility, refusal and transaction behavior; Edit geometry in plan calls the existing no-write `renovation.perspective('plan')` route. Existing/Planned/Work navigation, target selection, room-less context, Review behavior, schemas, costs, persistence, history and zero-versus-unknown handling are not changed.
- Remaining risks, conditional decisions and release impact: verification and browser evidence are incomplete pending the coordinator's slot and fixture issue. I00's deferred U4 chooser and non-drag arbitrary-corner limitation remain unchanged and cannot be reported as an accessibility pass.
- Reviewer findings, follow-up SHA and addressed threads: none.
- Confirmed merge SHA and cleanup result: pending review/integration; do not clean this worktree.
