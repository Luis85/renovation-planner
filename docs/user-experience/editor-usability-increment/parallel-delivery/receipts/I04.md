# I04 — Focus Renovate on work and information

- Packet and outcome: I04 presents a selected Renovate target as work and linked information rather than a structure/element geometry inspector. The Existing/Planned/Work routes remain the existing Renovation navigation, eligible targets gain a primary Add work item route, and Edit geometry in plan returns deliberately through the existing Plan perspective action.
- Assigned model/reasoning: `gpt-5.6-terra` / high.
- Status: review; the reviewer-found standalone Area geometry disclosure is Plan-only, its focused, canonical rotation-route and mandated regression gates passed, and the stacked PR remains open.
- Prerequisite PRs and confirmed merge SHAs: I00 is explicitly dispositioned through stack ancestry. Its reviewed tip `50e6a8dd30658d38e8f7474723fcedb00182a937` is an ancestor of this worktree HEAD. I02's reviewed parent is `0f45ff5e724cc49e8c9c9b8d2b7d64da7992b5b7`; the coordinator merged the reviewed I01/I02 stack into this branch at `f42fd2c26013182ace6274a5762374de7933ea1c`.
- Base SHA: `0f45ff5e724cc49e8c9c9b8d2b7d64da7992b5b7` (`codex/usability-i02-panels`).
- Branch/worktree: `codex/usability-i04-renovate-details` / `D:\Projects\renovation-planner\.worktrees\usability-i04-renovate-details`.
- PR URL and current commit: https://github.com/Luis85/renovation-planner/pull/177, opened against `codex/usability-i02-panels`; reviewer-fix commit `1a9bd77485e92bea3cf37b28e1a1d623f2815719` and type-narrowing fix `1e0beb5a7c93152213b3ea97a3394dd33e69d0e3` are pushed, with this receipt follow-up committed next.
- Owned files changed; any explicit ownership transfer: `RenovationEntry.vue`, `RenovationInspector.vue`, `styles/renovation.css`, this receipt, and new `tests/presentation/editor/usability/i04-renovate-details.test.ts`. The coordinator granted this PR a precise test-ownership extension for `tests/presentation/editor/rotationInspectorRoutes.test.ts`; I04 changes only its narrow-Renovate expectation from the removed default geometry controls to the preserved Edit layout route. `RenovationLinkedSummary.vue` was inspected but needs no code change: it already distinguishes unknown counts from known zero and exposes the concise existing linked-content routes.
- Shared-file integration request and resolution: none. Existing localized strings `renovation.add.work` and `editor.element.plan-geometry` describe the reused actions in both English and German; no locale change is needed.
- Commands run and exact results (include no-match/skipped limits): `git diff --check` passed with no output against `codex/usability-i02-panels...HEAD` and again after follow-ups. Impeccable mechanical detector over the three changed UI targets passed with `[]`. `npx eslint src/presentation/editor/renovation/RenovationEntry.vue src/presentation/editor/renovation/RenovationInspector.vue styles/renovation.css tests/presentation/editor/usability/i04-renovate-details.test.ts` completed with no errors; its earlier pass reported one warning that `styles/renovation.css` is ignored because no matching configuration is supplied. Follow-up targeted ESLint over `RenovationInspector.vue` and `i04-renovate-details.test.ts` completed with no output or errors. The mandated `npm run check:fast -- tests/presentation/editor/renovationOverview.test.ts tests/presentation/editor/renovationRoutes.test.ts tests/presentation/editor/roomlessRecords.test.ts` completed with exit status 0 before and after the follow-up, and again after the granted rotation-route assertion update. The three named files match 44 expanded test cases: 16 in `renovationOverview` (two `it.each` cases expand twice), 17 in `renovationRoutes`, and 11 in `roomlessRecords`. The focused `npm run check:fast -- tests/presentation/editor/usability/i04-renovate-details.test.ts` completed with exit status 0 before and after the follow-up and matches its 2 focused I04 regressions. The granted `npm run check:fast -- tests/presentation/editor/rotationInspectorRoutes.test.ts` completed with exit status 0 and matches its 2 canonical rotation-route cases. The configured reporters emitted no per-run console summary through this shell, so the matched counts are derived from the named test declarations and expansions, not fabricated test output.
- Browser evidence paths, fixture/URL, viewport, theme, locale and input method: opened the required production-equivalent URL on the isolated worktree harness, `http://localhost:5174/?view=plan-editor&reference&planning&fidelity&select=harness-kitchen&theme=light`, at the browser's default desktop viewport with English and no input. It loaded the harness, but after its fixture wait it continued to show the empty Ground floor state and no selected Kitchen, reference, planning content, or Renovate inspector. The same result occurred on the pre-existing default harness port. No selected Renovate light/dark/narrow capture is claimed.
- Native/AT/user-study checks actually performed: none.
- Unperformed checks and why: selected Plan/Renovate selection/camera equivalence, dark and narrow capture, German capture, room-less target browser capture, pointer/focus behavior, native Obsidian and AT checks remain unperformed. The harness route did not hydrate the documented selected fixture; no different browser or component specimen was substituted.
- Preserved command/history/geometry/state boundaries: Add work calls the existing `renovation.focus` then `renovation.edit` route, retaining its eligibility, refusal and transaction behavior; the visible control now requires a real zone or current/intended wall, opening, or element, so an unsupported target neither exposes nor dispatches Add work. Edit geometry in plan calls the existing no-write `renovation.perspective('plan')` route. The focused regressions prove a supported room-less wall keeps its Wall identity and selection, opens the existing work form, and returns to Plan without a vault write; and a standalone Area retains its geometry disclosure in Plan but not Renovate, where the explicit layout route remains. Existing/Planned/Work navigation, Review behavior, schemas, costs, persistence, history and zero-versus-unknown handling are not changed.
- Remaining risks, conditional decisions and release impact: browser evidence remains incomplete because the production-equivalent harness fixture did not hydrate; native/AT/participant acceptance is unperformed. I00's deferred U4 chooser and non-drag arbitrary-corner limitation remain unchanged and cannot be reported as an accessibility pass.
- Reviewer findings, follow-up SHA and addressed threads: coordinator review found that `standaloneZone` rendered `ObjectRotationControls` in Renovate because the disclosure was not perspective-gated. I04 changes only that `v-if` to Plan, preserves the Plan rotation behavior, and adds the standalone Area regression in `1a9bd77485e92bea3cf37b28e1a1d623f2815719`. Integration then found the same `vue-tsc` error on three Wave 2 branches: `editableStructureTarget` read `walls`, `openings`, and `elements` from optional `project.intended`. `1e0beb5a7c93152213b3ea97a3394dd33e69d0e3` skips an absent structure while preserving current-and-intended target eligibility. Finally, the coordinator granted I04 the named rotation-inspector test extension because its narrow-Renovate loop still expected one default rotation group for Room, Area, wall and opening. The revised loop asserts zero groups and the common Edit layout route, while the earlier Plan canonical-rotation assertions remain. No review thread is resolved here.
- Confirmed merge SHA and cleanup result: pending review/integration; do not clean this worktree.
