# I05 — Clarify starting and finishing Room creation

- Packet and outcome: I05 keeps the three existing empty-floor routes visible and distinct (Add rooms, optional reference plan, Start empty), moves ordinary room name and rectangular dimensions ahead of free-form shape entry, preserves invalid input and the accepted rectangle, and keeps the Inspector/task-banner completion doors synchronized. Keep adding remains explicit.
- Assigned model/reasoning: `gpt-5.6-luna` / high.
- Status: review; provisional implementation committed and pushed, with no PR opened on this initial turn.
- Prerequisite PRs and confirmed merge SHAs: I00 and I04 are explicitly dispositioned through stack ancestry. The reviewed I04 tip used here is `ded93356f8820fa6776cbe790ec955e2b4c781ce`.
- Base SHA: `ded93356f8820fa6776cbe790ec955e2b4c781ce` (`codex/usability-i04-renovate-details`).
- Branch/worktree: `codex/usability-i05-room-start-draft` / `D:\Projects\renovation-planner\.worktrees\usability-i05-room-start-draft`.
- PR URL and current commit: no PR opened by instruction; provisional implementation commit `bd85808f30e4cad217626f01eef20fdd1b0d699e` is pushed to the topic branch.
- Owned files changed; any explicit ownership transfer: `src/presentation/editor/reference/FloorStart.vue`, `src/presentation/editor/shell/NewRoomInspector.vue`, `styles/editor-new-room.css`, `styles/editor-creation-fidelity.css`, this receipt, and new `tests/presentation/editor/usability/i05-room-start-draft.test.ts`. No shared-file ownership transfer.
- Shared-file integration request and resolution: the existing locale table has `editor.resize.preview` but no localized room-specific “Last valid preview” label. The implementation uses the existing localized preview sentence plus `data-rp-preview-state="last-valid"` so the consumer remains compile-safe and truthful. Coordinator request: add `editor.room.last-valid-preview` to EN (`Last valid preview`) and DE (`Letzte gültige Vorschau`), then use it as the visible state label in the retained-preview presentation before final verification.
- Commands run and exact results (include no-match/skipped limits): `git diff --check` passed. `npx oxlint --deny-warnings` over the two changed Vue files and new I05 test passed. Targeted `npx eslint` over those same files passed with no output. Impeccable mechanical detector over the four changed UI targets returned `[]`. `npx vitest run tests/presentation/editor/usability/i05-room-start-draft.test.ts` passed: 1 file, 2 tests. `npx vue-tsc --noEmit` remains red only on pre-existing unrelated errors in `src/presentation/editor/renovation/RenovationEntry.vue:23` (three `TS18048` possibly-undefined diagnostics); no I05 diagnostics remain. Reserved `check:fast` was not run on this initial turn.
- Browser evidence paths, fixture/URL, viewport, theme, locale and input method: no production-equivalent browser capture was performed in this initial implementation slot; the focused jsdom test used the real mounted Plan Editor tree with the default English fixture and an empty-zone fixture for the three starts. No screenshot or browser evidence is claimed.
- Native/AT/user-study checks actually performed: none.
- Unperformed checks and why: the mandated `npm run check:fast -- tests/presentation/editor/shell/newRoomInspector.test.ts tests/presentation/editor/roomCreationWiring.test.ts tests/presentation/editor/roomCreation.e2e.test.ts`, browser light/dark/narrow captures, German capture, native Obsidian, AT and user-study checks remain for the coordinator's later verification slot. This turn was explicitly limited to static/focused validation before I03's heavy gate.
- Preserved command/history/geometry/state boundaries: no draft-store/parser, persistence, command/history, Select active state, Escape routing, busy/write behavior, or Cancel semantics changed. Existing `activateCreationEntry('room')`, `ReferenceAction`, `runtime.createRoom()`, and task-banner completion/cancel routes remain the action owners; UI changes only reorder and label presentation around them.
- Remaining risks, conditional decisions and release impact: the visible retained-preview copy is the existing generic localized preview string until the coordinator lands the requested room-specific EN/DE label. Visual geometry placement of the canvas label remains owned by the existing draft rendering components and was not changed under this packet's strict file boundary. No release artifacts or schemas changed.
- Reviewer findings, follow-up SHA and addressed threads: none yet.
- Confirmed merge SHA and cleanup result: pending review/integration; do not clean this worktree.
