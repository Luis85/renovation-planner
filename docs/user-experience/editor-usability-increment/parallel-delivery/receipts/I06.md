# I06 — Make reference source preparation approachable

- Packet and outcome: I06 makes the existing vault-file suggestions the primary source route. Each suggestion exposes the filename first and vault-relative path as context; the exact path input remains available for direct keyboard entry. PDF page and raster crop/rotation controls remain available through native disclosures, while the existing preview, loading, retry and source-error behavior stays owned by the parent setup workflow.
- Assigned model/reasoning: `gpt-5.6-luna` / high.
- Status: review; PR #181 is open against the reviewed I05 branch and remains unmerged.
- Prerequisite/base SHA: I05 was reviewed at `ea1e1af3d5325dbfa2a3d641192b5f18cc5fdf89` and merged into this branch as `57539b8a0abf3b75eb16501b9601783503d28d71`. That merge carries the reviewed I04/I03 stack and the locale seam; the original assigned I04 tip was `ded93356f8820fa6776cbe790ec955e2b4c781ce`.
- Branch/worktree: `codex/usability-i06-reference-prepare` / `D:\Projects\renovation-planner\.worktrees\usability-i06-reference-prepare`.
- PR URL and current commit: [PR #181](https://github.com/Luis85/renovation-planner/pull/181), opened with base `codex/usability-i05-room-start-draft`. Implementation commit `e77c25a7f21c40a45bbdc9ee5bf208e0d065c129` and semantic review-fix commit `863d6d892c6fdea87e27e67990aa72c986ceb94f` are pushed; this receipt follow-up is committed separately.
- Owned files changed; any explicit ownership transfer: `src/presentation/editor/reference/ReferencePrepare.vue`, `styles/editor-reference.css`, new `tests/presentation/editor/usability/i06-reference-prepare.test.ts`, and this receipt. No shared root, locale, CSS-index, setup-form, setup-model or existing-test files were changed.
- Shared-file integration request and resolution: none. Existing EN/DE strings cover the source, page, crop, rotation, load/retry and loading labels; no locale or interface change is needed.
- Actual source/preview/error behavior: the source list filters the existing `sources` prop case-insensitively, shows up to 20 supported candidates, and selecting a candidate updates the existing `path` model without adding a command or persistence step. The exact path input remains a native search field and the existing datalist is retained. The parent still calls `loadBackground`; a successful raster continues to mount `ReferencePreview`, loading continues to announce `editor.reference.loading`, and missing/unreadable/source-changed/submit errors continue to render through the parent’s existing status/error paths and existing load/retry action. No external upload or storage path was added.
- Commands run and exact results (include no-match/skipped limits): `npm exec -- vitest run tests/presentation/editor/usability/i06-reference-prepare.test.ts` passed 1 file and 3 tests after the review fix. The mandated `npm run check:fast -- tests/presentation/editor/referenceSetup.test.ts tests/presentation/editor/referenceWorkflow.e2e.test.ts` passed through oxlint, `vue-tsc -noEmit` and Vitest: 2 files and 53 tests, exit 0. This revalidated the existing reference input, page, crop/rotation disclosure, loading/retry, preview and source-error workflow on the stacked tree. Targeted `npm exec -- eslint src/presentation/editor/reference/ReferencePrepare.vue tests/presentation/editor/usability/i06-reference-prepare.test.ts --max-warnings 0`, `npm exec -- oxlint --deny-warnings` and `git diff --check` all passed with exit 0. The earlier provisional `vue-tsc` failure was resolved by the I05 merge; the current mandated gate is type-clean. The full build/check was not run.
- Browser evidence paths, fixture/URL, viewport, theme, locale and input method: none captured in this initial turn. No harness, browser, screenshot, or component capture is relabelled as production-equivalent evidence. The focused test ran in Vitest/jsdom on Windows with Node `v24.20.0` and npm `11.18.0`; it covers source selection/path context and disclosure presence/toggle, not native browser rendering.
- Native/AT/user-study checks actually performed: none.
- Unperformed checks and why: the production-equivalent harness at `?view=plan-editor&reference&planning&fidelity`, light/dark and narrow reflow, pointer/focus behavior in a browser, native Obsidian PDF/PNG/JPEG loading, screen-reader behavior and participant acceptance remain unperformed. The final I16 full gate remains coordinator-owned; no browser/native/AT evidence is relabelled from jsdom.
- Preserved command/history/geometry/state boundaries: selecting a suggestion only updates the existing path model; loading still uses the existing `load` emit and parent loader; preparation remains a draft until the existing final dispatch. No schema, repository, persistence, transaction, history, scale or geometry math changed. Native buttons, the exact path field, PDF page input, crop inputs, rotation input, load selector and existing models/emits remain available.
- Reviewer finding and resolution: coordinator review identified that `role=listbox`/`role=option` implied unsupported arrow-key listbox behavior. The fix in `863d6d892c6fdea87e27e67990aa72c986ceb94f` changes the container to a labeled native `role=group`, removes option semantics, expresses selection with native-button `aria-pressed`, and uses native `disabled` while paused. The focused regression now locks this contract without introducing roving-key state or changing props/models/emits.
- Remaining risks, conditional decisions and release impact: the custom vault list and disclosures still need real harness/native verification. The design remains dependent on the parent’s existing preview/error state because expanding parent ownership was outside this packet.
- Confirmed merge SHA and cleanup result: pending review/integration; do not clean this worktree.
