# I07 receipt — Clarify points, known distance and rescale review

- Packet and outcome: I07; point-progress and known-distance-first scale step, exact-coordinate disclosure, and a preview-versus-final review boundary with conditional rescale acknowledgement.
- Assigned model/reasoning: gpt-5.6-terra / high.
- Status: review — final packet verification is green and the branch is ready for a stacked PR.
- Prerequisite PRs and confirmed merge SHAs: the final green I04 correction and I03 CI repair are integrated through the current I06 head `dfba682ba23b159b4e818ca3bef8ca794864312b`; it remains part of the unmerged I06 → I07 → I08 → I09 chain. I00/I06 merge-state verification is coordinator-owned for this stacked delivery.
- Base SHA: `dfba682ba23b159b4e818ca3bef8ca794864312b`.
- Branch/worktree: `codex/usability-i07-reference-scale-review` / `D:\Projects\renovation-planner\.worktrees\usability-i07-reference-scale-review`.
- PR URL and current commit: https://github.com/Luis85/renovation-planner/pull/184; the branch tip includes this durable PR record.
- Owned files changed; any explicit ownership transfer: packet-owned `ReferenceSetupForm.vue`, new `ReferenceMeasure.vue`, `ReferenceReview.vue`, `styles/editor-reference.css`, `tests/presentation/editor/usability/i07-reference-scale-review.test.ts`, and this receipt. The coordinator additionally accepted the CI-owned structural extension to `StructureInspector.vue`, `StructurePlanActions.vue`, and new `useStructureInspectorTarget.ts` to remove an inherited Fallow clone without changing behavior.
- Shared-file integration request and resolution: requested seven reference-review labels; coordinator committed EN/DE aggregate locale entries in `9b3892968ecb81fa178eb604d4e7e708bdd2e080` (`Reserve reference review copy`). I07 consumes the keys without changing locale files.
- Commands run and exact results (include no-match/skipped limits): `npm test -- --run tests/presentation/editor/usability/i07-reference-scale-review.test.ts --pool=threads --maxWorkers=1` passed: 1 file / 1 test. Initial mandated `npm run check:fast -- tests/presentation/editor/referenceSetup.test.ts tests/presentation/editor/referenceViewportControls.test.ts tests/presentation/editor/referenceWorkflow.e2e.test.ts tests/presentation/editor/referencePdfCleanup.test.ts` stopped at oxlint on an unused I07 test import; the import was removed. After the corrected parent stack was integrated, `npm run check:fast -- tests/presentation/editor/referenceSetup.test.ts tests/presentation/editor/referenceViewportControls.test.ts tests/presentation/editor/referenceWorkflow.e2e.test.ts tests/presentation/editor/referencePdfCleanup.test.ts tests/presentation/editor/usability/i07-reference-scale-review.test.ts` passed: `oxlint`, `vue-tsc`, 5 matched files and all 69 tests were green in 46.25 s. CI run `34770961957` then failed all four verify jobs at ESLint only: 17 auto-fixable Vue indentation errors and four single-line-content warnings in the two I07 components. The repository ESLint autofix was applied to those exact files; `npx eslint ... --max-warnings 0` passed, and the same five-file `check:fast` gate passed again with all 69 tests in 31.99 s. The next CI run `34772188334` passed all 898 files / 9,946 tests and coverage, then Fallow found one inherited five-line structure clone and `ReferenceSetupForm` at cognitive 17. Extracting `ReferenceMeasure` and the shared structure-target composable removed both without suppressions. The final six-file `check:fast` gate (the five I07 suites plus `structureJourney.test.ts`) passed: 6 files / 72 tests. Targeted ESLint and `git diff --check` passed; `fallow dupes` reported no duplication, and a no-coverage complexity run no longer listed `ReferenceSetupForm`. The final Impeccable detector over the extracted UI seams returned `[]`.
- Browser evidence paths, fixture/URL, viewport, theme, locale and input method: production-equivalent harness `http://127.0.0.1:5174/?view=plan-editor&reference&planning&fidelity`, 1280×720, dark, English. With `scan.png`, the observed scale step presented A/B pending states, known distance before a collapsed `Exact point coordinates` native disclosure; expanding it exposed four named numeric controls. Entering A `(100,100)`, B `(300,100)` updated the visible selected states; entering `2` and continuing showed scale `10 mm per source pixel`, the preview-only notice, and `Finish setup`. The fixture had no existing geometry, so the conditional rescale panel correctly did not appear. Browser interaction was accessibility-tree control activation and value entry; no native Obsidian or screen-reader run was performed.
- Native/AT/user-study checks actually performed: none.
- Unperformed checks and why: native Obsidian/PDF and AT checks require their named environments. CI and PR review remain pending; the final packet-level automated gate is complete.
- Preserved command/history/geometry/state boundaries: retained `setupMeasurement`, preview/source transforms, original source, rescale-scope predicate, default unchecked acknowledgement, single final `dispatch`, cancellation route, error/retry draft state, and existing back behavior. No ReferencePreview or setup-state edit.
- Remaining risks, conditional decisions and release impact: rescale review’s conditional harness state was not browser-exercised because the fidelity fixture is empty; existing workflow coverage remains the boundary evidence. Full matrix CI and final integration/native verification remain later stack gates.
- Reviewer findings, follow-up SHA and addressed threads: CI run `34770961957` found template formatting, and run `34772188334` found the Fallow complexity/clone pair after every test and coverage passed. Both are structurally corrected; the final follow-up SHA is reflected by the PR head after push. No review threads are open.
- Confirmed merge SHA and cleanup result: not merged; worktree retained.
