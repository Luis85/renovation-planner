# I08 — Adapt the bottom taskbar and current-task guidance

- Packet and outcome: Plan keeps the shared Select/Pan taskbar controls and exposes layout Add; Renovate retains Select/Pan while hiding layout Add. The Room task banner uses the existing `runtime.roomDraft` and `runtime.createRoom()` state to name invalid and saving outcomes, retain outcome-specific completion, and make its Cancel unavailable while a Room write is in flight.
- Assigned model/reasoning: `gpt-5.6-terra` / high.
- Status: review (provisional); awaiting the coordinator's serialized I07 integration, shared wiring patch and verification slot.
- Prerequisite PRs and confirmed merge SHAs: dispatched stacked baseline through I06 at `e5dbc6749aa4b0302bd93c2e899aeb73aac291ac`; I07 is the active serialized predecessor and is not yet integrated into this worktree.
- Base SHA: `e5dbc6749aa4b0302bd93c2e899aeb73aac291ac` (`codex/usability-i06-reference-prepare`).
- Branch/worktree: `codex/usability-i08-taskbar` / `D:\Projects\renovation-planner\.worktrees\usability-i08-taskbar`.
- PR URL and current commit: no PR is opened; provisional commit `d6aeaed95a918d26fa9198256d7a988c147e5b48` is pushed for the coordinator to rebase/merge I07 forward before final checks.
- Owned files changed; any explicit ownership transfer: `src/presentation/editor/shell/FloatingPrimaryActions.vue`, `src/presentation/editor/shell/TemporaryToolBanner.vue`, `styles/editor-task-bar.css`, this receipt, and new `tests/presentation/editor/usability/i08-taskbar.test.ts`. `TaskDrawingControls.vue` was inspected and left unchanged: its existing `FreeShapeRoomAction` already reads the same shared room-draft busy state and needs no second producer.
- Shared-file integration request and resolution: **pending.** In `src/presentation/editor/PlanEditorRoot.vue`, change `showAddMenu` to require `renovationSession.perspective === 'plan'`, guard `onOpenAdd()` with an early return when the perspective is not Plan, and add a perspective watcher that sets `addMenuOpen.value = false` when leaving Plan. This prevents the existing canvas-context Add route or a previously-open menu from exposing layout Add in Renovate. In `src/presentation/editor/shell/NewRoomInspector.vue`, derive `roomBusy` from `runtime.roomDraft.submitting`, bind `aria-disabled` on `.rp-new-room__cancel`, and guard its click handler with the same value before `runtime.cancelActiveTask()`. This makes Details and the banner share the non-abortable write boundary without changing the command or draft store. Update the affected I05/temporary-banner tests to assert that both Cancel doors leave the Room task intact during `submitting` and continue to leave it when the draft is invalid but not busy.
- Commands run and exact results (include no-match/skipped limits): `git diff --check` passed. Two focused Vitest invocations were started (`tests/presentation/editor/usability/i08-taskbar.test.ts` alone and that test with the packet's three target suites), but neither returned a completion summary while the shared I07 verification process was active; neither is claimed as passed. The packet-prohibited `npm run check:fast` was not run.
- Browser evidence paths, fixture/URL, viewport, theme, locale and input method: no valid I08 browser capture. The production-equivalent harness at `http://127.0.0.1:5173/?view=plan-editor&reference&planning&fidelity` was observable in English/default theme, but it was an already-running server and continued to show Add in Renovate, so it was not treated as evidence for this worktree. An isolated `127.0.0.1:5180` harness did not bind before inspection and was stopped. No screenshot is claimed.
- Native/AT/user-study checks actually performed: none.
- Unperformed checks and why: focused Vitest completion, the packet `check:fast` command, light/dark/narrow browser captures, German browser capture, native Obsidian, AT and user study remain pending the I07 serialized integration, verification-slot grant and the shared Details/root patch.
- Preserved command/history/geometry/state boundaries: no command dispatch, persistence, history, geometry, selection, task-token, or cancellation implementation changed. The banner still calls the existing `runtime.createRoom()` and `runtime.cancelActiveTask()` doors; it only prevents its Cancel control from claiming an in-flight write can be aborted.
- Remaining risks, conditional decisions and release impact: until the coordinator lands the specified root and Details wiring, a canvas-context Add route may still open the layout menu in Renovate and Details Cancel may still leave a busy Room task. The branch is intentionally not ready for PR/final verification until those seams are resolved.
- Reviewer findings, follow-up SHA and addressed threads: none.
- Confirmed merge SHA and cleanup result: pending; do not clean this worktree.
