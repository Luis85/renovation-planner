# I08 — Adapt the bottom taskbar and current-task guidance

- Packet and outcome: Plan keeps the shared Select/Pan taskbar controls and exposes layout Add; Renovate retains Select/Pan, replaces layout Add with the existing eligible `Add work` route, and exposes `More actions` as a non-writing Details reveal. The Room task banner uses the existing `runtime.roomDraft` and `runtime.createRoom()` state to name invalid and saving outcomes, retain outcome-specific completion, and make both Cancel doors unavailable while a Room write is in flight.
- Assigned model/reasoning: `gpt-5.6-terra` / high.
- Status: review; final packet verification is green and the branch is ready for a stacked PR.
- Prerequisite PRs and confirmed merge SHAs: final I07 head `a0df7ccfb` is integrated through merge `c1fb8d6478c0c2f530a218711b840d906a2d8c3e`; I08 remains in the unmerged I07 → I08 → I09 chain.
- Base SHA: `a0df7ccfb` (`codex/usability-i07-reference-scale-review`).
- Branch/worktree: `codex/usability-i08-taskbar` / `D:\Projects\renovation-planner\.worktrees\usability-i08-taskbar`.
- PR URL and current commit: https://github.com/Luis85/renovation-planner/pull/185; the branch tip includes this durable PR record.
- Owned files changed; any explicit ownership transfer: packet-owned `src/presentation/editor/shell/FloatingPrimaryActions.vue`, `src/presentation/editor/shell/TemporaryToolBanner.vue`, `styles/editor-task-bar.css`, this receipt, and new `tests/presentation/editor/usability/i08-taskbar.test.ts`; integrator-owned extensions `src/presentation/editor/PlanEditorRoot.vue`, `src/presentation/editor/shell/NewRoomInspector.vue`, `src/presentation/editor/renovation/renovationActions.ts`, `src/presentation/editor/renovation/RenovationEntry.vue`, and the intentional assertion update in `tests/presentation/editor/shell/temporaryToolBanner.test.ts`. `TaskDrawingControls.vue` was inspected and left unchanged: its existing `FreeShapeRoomAction` already reads the same shared room-draft busy state and needs no second producer.
- Shared-file integration request and resolution: resolved in this final I08 commit. `PlanEditorRoot.vue` now closes and guards layout Add outside Plan, so canvas-context and stale-menu routes cannot expose it in Renovate. `NewRoomInspector.vue` and `TemporaryToolBanner.vue` read the same `roomDraft.submitting` state and guard both Cancel doors. `renovationActions.ts` now owns one `canAddWork`/`addWork` pair used by both Details and the bottom taskbar, preventing duplicated eligibility or command dispatch. `More actions` calls the existing workspace `revealInspector()` route and writes no project state.
- Commands run and exact results (include no-match/skipped limits): the first final packet gate exposed one intentional stale exact-class assertion after an invalid Room gained its explicit modifier; the assertion now preserves the placement check while expecting `rp-task-banner--invalid` only for `draw-room`. The corrected `npm run check:fast -- tests/presentation/editor/taskBannerOperationBoundaries.test.ts tests/presentation/editor/shell/temporaryToolBanner.test.ts tests/presentation/editor/inputInteractions.test.ts tests/presentation/editor/usability/i08-taskbar.test.ts` passed: 4 files and 25 tests. After the full Renovate taskbar route was integrated, `npm run check:fast -- tests/presentation/editor/taskBannerOperationBoundaries.test.ts tests/presentation/editor/shell/temporaryToolBanner.test.ts tests/presentation/editor/inputInteractions.test.ts tests/presentation/editor/renovationOverview.test.ts tests/presentation/editor/usability/i08-taskbar.test.ts` passed: `oxlint`, `vue-tsc`, 5 files and all 41 tests were green in 30.73 s. `git diff --check` passed. The final Impeccable detector over all changed UI targets returned `[]`.
- Browser evidence paths, fixture/URL, viewport, theme, locale and input method: no valid I08 browser capture. The production-equivalent harness at `http://127.0.0.1:5173/?view=plan-editor&reference&planning&fidelity` was observable in English/default theme, but it was an already-running server and continued to show Add in Renovate, so it was not treated as evidence for this worktree. An isolated `127.0.0.1:5180` harness did not bind before inspection and was stopped. No screenshot is claimed.
- Native/AT/user-study checks actually performed: none.
- Unperformed checks and why: light/dark/narrow browser captures, German browser capture, native Obsidian, AT and user study remain pending later verification packets. CI and PR review are pending; packet-level automated verification is complete.
- Preserved command/history/geometry/state boundaries: no command dispatch, persistence, history, geometry, selection, task-token, or cancellation implementation changed. The banner still calls the existing `runtime.createRoom()` and `runtime.cancelActiveTask()` doors; it only prevents its Cancel control from claiming an in-flight write can be aborted.
- Remaining risks, conditional decisions and release impact: full matrix CI and later native/accessibility verification remain. Renovate `More actions` deliberately reveals the existing Details surface instead of inventing a second contextual menu; `Add work` is shown only when the centralized existing-target eligibility check passes.
- Reviewer findings, follow-up SHA and addressed threads: full-stack lint found that adding the centralized work helpers pushed `createRenovationActions` to 107 lines against the 100-line limit. The helpers are now produced by a small external adapter, preserving one eligibility/dispatch path; targeted ESLint passed with zero warnings, and the I08 plus Renovation overview gate passed again (2 files, 18 tests). CI run `34773995850` then passed 8,947 cases and failed only the pre-I08 test that still expected layout Add in Renovate; it now verifies keyboard-reachable Work/Details actions, selection retention, and their Review removal. The same log showed the new `ellipsis` icon was absent from the pinned host icon set, so `More actions` now uses the supported `panels-top-left` icon and the focused test guards against a missing icon. `npm run check:fast -- tests/presentation/editor/renovateRoomManipulation.test.ts tests/presentation/editor/usability/i08-taskbar.test.ts` passed: 2 files / 6 tests. CI run `34775069566` then passed all 9,948 tests and coverage but reported `TemporaryToolBanner` at cognitive 24. The state-message and action clusters are now extracted into `TaskBannerMessages` and `TaskBannerActions`, preserving IDs, live-region roles, state and command doors without a suppression. The final five-file gate passed (29 tests); targeted ESLint, Fallow duplication, a no-coverage complexity check and the Impeccable detector are clean. I15 observation F-01 found the four-action Renovate taskbar cropped at the supported 400px floor. Every button now has an explicit accessible name and a wrapped visual label; only the 400–439px band hides those labels and renders four visible 44px icon targets, while 440px+ retains labels and 399px remains the unsupported shell. The focused taskbar gate passed (2 files / 6 tests) and the Impeccable detector returned `[]`. The final follow-up SHA is reflected by the PR head after push; no review threads are open.
- Confirmed merge SHA and cleanup result: pending; do not clean this worktree.
