# I09 — Make overlapping targets understandable

- Packet and outcome: I09 keeps I00's U4 chooser decision deferred. The existing canvas-context route now names the resolver's current target and explains the existing Alt overlap cycle; the shell's short live status announces the same named route after a single-target change. No chooser, candidate list, hit rank or selection-model API is added.
- Assigned model/reasoning: `gpt-5.6-terra` / high (per dispatch).
- Status: provisional handoff. This branch is stacked at reviewed I06 tip `e5dbc6749aa4b0302bd93c2e899aeb73aac291ac`; the coordinator serializes I06 → I07 → I08 → I09 before granting I09's final gate or opening its PR.
- Prerequisites and conditional disposition: I00's reviewed contract explicitly defers the named U4 chooser because no native/novice evidence records a failure of the current route. I03 is present through the reviewed stack ancestry. This packet therefore makes only the approved clarity improvement.
- Owned production files changed: `CanvasContextMenu.vue`, `SelectionGuidance.vue`, and `styles/editor-context-menu.css`. Added focused regression: `tests/presentation/editor/usability/i09-selection-guidance.test.ts`.
- Shared-file integration request and resolution: I09 requested `editor.input.current-target` and `editor.input.overlap-cycle-guidance` in EN/DE, because the existing `editor.input.context` says only `Plan actions` and no existing key truthfully explains Alt overlap cycling. The coordinator added EN `Current target: {target}` / `Alt-click to select another overlapping item.` and DE `Aktuelles Ziel: {target}` / `Halten Sie beim Klicken Alt gedrückt, um ein anderes überlappendes Element auszuwählen.` in the paired `input.ts` locale modules. The integrator also updates the existing context-menu title assertion to the complete current-target-plus-Alt header; I09 does not claim ownership of that existing test.

## Behaviour and preserved boundaries

- Right-click/context invocation still calls the existing `resolveSelectionTarget(...)` route. Without Alt it names the resolved target; with Alt it retains the resolver's existing cycle of the same admitted candidates. The current target is still selected/focused through the existing `selectContext` path.
- Escape and focus restoration remain owned by the existing context-menu close route. The added regression checks that Escape returns focus to the canvas without changing the target; it also checks an overlapping Door → Alt → host Wall cycle without adding a chooser.
- `resolveSelectionTarget.ts` and `selection-store.ts` were inspected and intentionally unchanged. Existing locked/hidden admission (`canvasCandidates`), group expansion, handle priority, body ranking, history and persistence boundaries stay unchanged.
- The live status retains its existing one-time clear-selection announcement. It now also makes a changed single selected target understandable to assistive technology, then clears on the same short timer; timer cleanup prevents an older announcement from clearing a newer one.

## Verification and evidence

- Static checks run on Windows: `npx eslint src/presentation/editor/selection/CanvasContextMenu.vue src/presentation/editor/shell/SelectionGuidance.vue styles/editor-context-menu.css tests/presentation/editor/usability/i09-selection-guidance.test.ts` — passed. `npx vue-tsc --noEmit` — passed.
- Focused test attempted: `npx vitest run tests/presentation/editor/usability/i09-selection-guidance.test.ts` — did not begin; Node terminated with `FATAL ERROR: NewSpace::EnsureCurrentCapacity Allocation failed - JavaScript heap out of memory` while the shared heavy verification slot was active. It is not a passing or failing test result and was not retried.
- Deliberately not run: `npm run check:fast` and the packet's shared targeted gate, because I07 owns the one heavy verification slot. The coordinator must run the final shared gate after serializing the stack.
- Browser/native/AT/user evidence: no browser, native Obsidian, assistive-technology or novice T6 study was performed. The packet mockup and hybrid gallery were used only as hierarchy guidance; no visual or user-study acceptance is claimed.
- Remaining risks and release impact: the visible route has an automated regression once the shared test run is available, but production-equivalent browser interaction, native behavior and AT output remain unperformed. I00's deferred U4 chooser remains deferred and this increment cannot be cited as T6 study evidence or an accessibility pass.
