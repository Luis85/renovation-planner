# I11 — Clarify opening identity and host-relative properties

- Packet and outcome: I11; Door/window/opening Details now lead with the opening kind, host wall, room/no-room context and floor name, followed by the supported width, height, offset and sill readouts. Plan keeps hinge/swing in the existing secondary edit form, and openings get a direct Renovate hand-off.
- Assigned model/reasoning: `gpt-5.6-luna` / high (per dispatch).
- Status: review; final packet verification is green and the branch is ready for its stacked PR.
- Base SHA: `a2a961bd8` (`codex/usability-i10-dimensions-feedback`), integrated through merge `2ef70a075`.
- Provisional implementation SHA: `7d14789f3`; the final correction/receipt SHA is reflected by the PR head after push.
- Branch/worktree: `codex/usability-i11-opening-details` / `D:\Projects\renovation-planner\.worktrees\usability-i11-opening-details`.
- PR URL: https://github.com/Luis85/renovation-planner/pull/189; the branch tip includes this durable PR record.
- Production files changed: `src/presentation/editor/structure/StructureInspector.vue`, `styles/editor-structure.css`. Focused regression added at `tests/presentation/editor/usability/i11-opening-details.test.ts`.
- Shared-file integration request: resolved during serialization. I11's reordered Details were reconciled with I07's shared `useStructureInspectorTarget` helper without changing root composition, locale aggregates or the CSS index. The existing `StructureInspector` mount and `runtime.renovation.focus(roomId, mode, targetId)` route remain the required interfaces; no new key, prop, selector registration or locale text was required.

## Behaviour and preserved boundaries

- Opening Details uses the existing `Opening` identity and host attachment. Host labels derive from the current wall ordering; a missing host falls back to the persisted host id rather than inventing a free-standing opening or reassignment action.
- Room context derives from boundaries containing the host wall. The inspector explicitly says `No room` when no enclosing boundary is available, while the Renovate route passes the selected opening id through the existing navigation action so room-less structural targets remain reachable.
- Width, height, offset and sill remain read-only projection values in Details. `Edit measurements` still opens the existing validated `StructureEditForm`, whose opening fields and `OpeningSwingFields` preserve current parsing, bounds checks, preview/apply transaction granularity and hinge/swing semantics.
- The new `Renovate: Door|Window|Opening` button calls only `runtime.renovation.focus(..., 'overview', opening.id)` and creates no vault write or history entry. Existing Move, Edit measurements, More actions, material and Delete routes remain mounted.
- No domain, schema, command, geometry, host reassignment, rename command, or persisted offset meaning changed.

## Verification and evidence

- `npx oxlint --deny-warnings src/presentation/editor/structure/StructureInspector.vue tests/presentation/editor/usability/i11-opening-details.test.ts` — passed.
- `npx eslint src/presentation/editor/structure/StructureInspector.vue styles/editor-structure.css tests/presentation/editor/usability/i11-opening-details.test.ts --max-warnings 0` — passed.
- `npx vue-tsc --noEmit` — passed.
- `git diff --check` — passed.
- Impeccable context was loaded once for the worktree. The required final detector ran once over `StructureInspector.vue` and `styles/editor-structure.css` and returned `[]`.
- Focused test gate: earlier attempts aborted before Vitest output with `Fatal error. Releasing the double mapped memory failed`, so no pass was claimed. On the serialized I10 parent, the first complete gate exposed a branded `EntityId` test-fixture type mismatch; after correction, all 38 tests passed but Vitest reported an unhandled rejection because the new async Renovate route read `event.currentTarget` after `nextTick`. Capturing the opener before the await fixed that real focus bug. The final `npm run check:fast -- tests/presentation/editor/structureSelection.test.ts tests/presentation/editor/openingMove.test.ts tests/presentation/editor/usability/i11-opening-details.test.ts` passed: `oxlint`, `vue-tsc`, 3 files and all 38 tests were green in 21.96 s. The final Impeccable detector returned `[]` and `git diff --check` passed.
- Browser/native/AT/user evidence: none claimed. The production-equivalent harness, light/dark/narrow German rendering, native Obsidian, screen reader and novice study remain unperformed and belong to later verification/integration packets.

## Remaining risks and release impact

- The new focused regression covers hosted identity/readout ordering, room-less context, real room context and the no-write Renovate route; it now passes on the final parent.
- Visual QA used the UX-06 gallery image and incumbent Obsidian-token conventions; no connected browser capture is being relabeled as acceptance evidence.
- I00's conditional U4 chooser disposition and the existing arbitrary-corner limitation remain unchanged.
- Reviewer findings and follow-up: serialization resolved the one `StructureInspector` conflict by retaining I11's opening hierarchy on I07's shared target state. ESLint autofix cleared 12 Vue single-line-content warnings, and the async opener fix removed the unhandled focus rejection. I16's fully green test/coverage phase then reported `StructureInspector` at cognitive 31 plus a duplicate fact/material cluster. Opening identity/context/facts are now isolated in `OpeningDetails`, and wall/opening material rows share `StructureMaterialFacts`; the wall-only `StructureFacts` no longer carries an unused opening branch. The structure gate passed (3 files / 23 tests), Fallow reports no duplication, targeted ESLint and `git diff --check` pass, and the Impeccable detector returned `[]`. No review thread is open.
