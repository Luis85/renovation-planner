# I13 — Make draft, saving and recovery state truthful

- Packet and outcome: a reference-scale status is now drawn only when the loaded plan has a reference background, so a floor authored from typed dimensions no longer shows the irrelevant “Scale not set” warning. Draft recovery exposes its real branch (`read-failed` or `unconfirmed`), retains its target, and continues to remove the read retry after an uncompensated write. The persistent warning region announces changed rows without re-announcing retained siblings.
- Assigned model/reasoning: `gpt-5.6-terra` / high (per dispatch).
- Status: review; the coordinator’s reserved status-bar fixture update is present, and the final packet gate remains to be rerun in the shared verification slot.
- Prerequisite PRs and confirmed merge SHAs: supplied final I12 stack head `4305750c5` (`Record I12 pull request`). This receipt makes no merge claim for I00, I07, I08 or I12 beyond the prepared branch provided to this session.
- Base SHA: `4305750c5`; coordinator integration commit: `82edeb82c` (`Align scale status fixtures`).
- Branch/worktree: `codex/usability-i13-save-recovery` / `D:\Projects\renovation-planner\.worktrees\usability-i13-save-recovery`.
- PR URL and current commit: pending push.
- Owned files changed; any explicit ownership transfer: `src/presentation/editor/shell/StatusBar.vue`, `src/presentation/editor/shell/PersistentWarningStrip.vue`, `src/presentation/editor/forms/DraftRecovery.vue`, `styles/editor-status.css`, and new `tests/presentation/editor/usability/i13-save-recovery.test.ts`. No ownership transfer.
- Shared-file integration request and resolution: I13 requested a serialized update to reserved `tests/presentation/editor/shell/statusBar.test.ts`: assert no `.rp-editor-scale` for `FIXTURE_PLAN`; give calibrated and uncalibrated scale cases a real image background; and remove the constrained-layout expectation that scale stays visible without a reference. The coordinator landed it as `82edeb82c` (`Align scale status fixtures`) while I13 left the reserved test untouched. No locale request: existing localized scale, recovery and source-note strings describe the supported states without adding a new term that could blur known failure and unknown outcome.
- Commands run and exact results (include no-match/skipped limits): `npx eslint src/presentation/editor/shell/StatusBar.vue src/presentation/editor/shell/PersistentWarningStrip.vue src/presentation/editor/forms/DraftRecovery.vue tests/presentation/editor/usability/i13-save-recovery.test.ts` — passed. `npx vitest run tests/presentation/editor/usability/i13-save-recovery.test.ts --reporter=verbose` — passed (3 tests); Konva emitted its existing six/seven-layer performance warnings. `npx vue-tsc --noEmit` — passed. `impeccable.cmd detect --json src/presentation/editor/shell/StatusBar.vue src/presentation/editor/shell/PersistentWarningStrip.vue src/presentation/editor/forms/DraftRecovery.vue styles/editor-status.css` — passed (`[]`). `git diff --check` — passed. Before the fixture correction, `npx vitest run tests/presentation/editor/shell/statusBar.test.ts --reporter=verbose` failed only the three superseded no-background scale assertions; its remaining 8 tests passed. After `82edeb82c`, repeated status-bar and planned packet-gate Vitest attempts reached `RUN` but the terminal runner returned before a final result, so no post-integration pass is claimed.
- Browser evidence paths, fixture/URL, viewport, theme, locale and input method: none. The production-equivalent harness command (`npm run harness`, intended route `?view=plan-editor&reference&planning&fidelity`) could not remain attached in the terminal runner, so no capture, viewport, theme, locale, pointer or keyboard result is claimed.
- Native/AT/user-study checks actually performed: none.
- Unperformed checks and why: browser harness attachment, native Obsidian, assistive technology and novice U2/U6/U7/U8 evaluation are unavailable in this session. The full planned packet gate awaits a terminal run that returns a final Vitest result in the shared verification slot.
- Preserved command/history/geometry/state boundaries: no command, repository, history, geometry, draft-store or recovery owner changed. A stale retry remains a read-only refresh from the existing runtime route; DraftRecovery offers no retry once `unrecoveredWrite` is true, and only its existing source-note inspection action remains. A known read failure still retains the original draft and its target. No failure category is reclassified and no write is replayed or synthesized.
- Remaining risks, conditional decisions and release impact: a typed plan with no reference now correctly suppresses a scale sentence, and the reserved test fixture was aligned in `82edeb82c`; a returning full packet gate is still required. Browser/native/AT/user-study evidence remains required before release acceptance.
- Reviewer findings, follow-up SHA and addressed threads: none.
- Confirmed merge SHA and cleanup result: not merged; not applicable.
