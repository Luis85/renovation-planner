# I14 receipt — Prevent accidental geometry editing in Renovate

- Packet and outcome: I14. Renovate now suppresses selected Room vertex and wall endpoint
  affordances, and arrow-key nudging is a no-op at the canvas boundary. The required pointer
  mutation guard is deliberately deferred to the integrator request below; this receipt is not
  a claim that CSS/visual suppression alone completes I14.
- Assigned model/reasoning: inherited session model / high.
- Status: conditional-deferred pending the reserved `SelectTool` and runtime boundary change.
- Prerequisite PRs and confirmed merge SHAs: branch supplied at I12 head `4305750c5`; the
  integrator owns prerequisite merge admission for I00, I04, I08, I10 and I11.
- Base SHA: `4305750c5` (`Record I12 pull request`).
- Branch/worktree: `codex/usability-i14-mode-input-guard` /
  `D:\Projects\renovation-planner\.worktrees\usability-i14-mode-input-guard`.
- PR URL and current commit: provisional branch only; no PR requested or opened.
- Owned files changed: `InteractionLayer.vue`, `StructureLayer.vue`, `EditorSurface.vue`, and
  `tests/presentation/editor/usability/i14-mode-input-guard.test.ts`.

## Shared-file integration request

The remaining guard cannot be implemented in an owned visual layer: `SelectTool.pointerDown`
starts Room body/vertex drags, wall endpoint edits, element/label/rotation moves and group moves
even while `InteractionLayer` has hidden the handles. `EditorSurface` cannot distinguish a
marquee selection from one of those gestures without duplicating the tool's hit-testing.

1. Add a `canMutateGeometry: () => boolean` dependency to
   `src/presentation/editor/tools/select-tool.ts`. In Renovate it must leave ordinary target
   selection and marquee selection available, but block every gesture start and cancel an
   in-flight geometry gesture if the perspective changes. This covers zone body/vertex,
   selected-group, wall endpoint, element, label and rotation starts; no preview, command or
   history entry may result.
2. In `src/presentation/editor/tools/registerEditorTools.ts`, source that predicate from the
   leaf's `useRenovationSession()` and pass `() => session.perspective === 'plan'` to
   `SelectTool`. This keeps the decision with the registered tool rather than manufacturing a
   second hit-test in `EditorSurface`.
3. Add the same perspective gate to `src/presentation/editor/nudge.ts` and wire it in
   `src/presentation/editor/runtime.ts` when constructing `createNudgeSelectionAction`. The
   owned `EditorSurface` wrapper is an early UI guard; the action itself needs the authoritative
   no-write boundary for any future caller.
4. Make editable scalar dimension controls in
   `src/presentation/editor/resize/RoomDimensionLabels.vue` Plan-only. CI DOM evidence found
   `[data-rp-dimension="width"]` and `[data-rp-dimension="depth"]` still rendered in Renovate;
   purely informational edge measurements may remain. Add a Renovate assertion that those
   dimension-edit buttons are absent while the selected Room/context remains visible.
5. Update the now-obsolete success expectations in
   `tests/presentation/editor/renovateRoomManipulation.test.ts`, and add focused pointer
   assertions for Room body/vertex and wall endpoint in Renovate. Verify selection, pan,
   navigation, eligible renovation actions, the existing draft confirmation/busy guard, and the
   Review-return snapshot stay unchanged.

- Commands run and exact results:
  - `npx vitest run tests/presentation/editor/usability/i14-mode-input-guard.test.ts --reporter=verbose`
    — passed: 1 file, 2 tests. Konva emitted its known seven-layer performance warning.
  - `npm run check:fast -- tests/presentation/editor/renovationWorkflow.test.ts tests/presentation/editor/renovationRetiredActions.test.ts tests/presentation/editor/inputInteractions.test.ts tests/presentation/editor/escapeRouting.test.ts`
    — passed: 4 files, 20 tests; duration 48.56 s.
  - `impeccable.cmd detect --json src/presentation/editor/layers/InteractionLayer.vue src/presentation/editor/structure/StructureLayer.vue src/presentation/editor/surface/EditorSurface.vue`
    — `[]` (no findings).
- Browser evidence: the requested production-equivalent harness route was opened at
  `?view=plan-editor&reference&planning&fidelity&select=harness-kitchen`, light English, then
  switched to Renovate. Its supplied reference/planning fixture rendered an empty floor and
  never resolved the requested selected Room, so it could not be used as handle evidence.
- Native/AT/user-study checks actually performed: none.
- Unperformed checks and why: screenshot/gesture evidence is deferred because the browser
  fixture did not provide a selected Room and the remaining `SelectTool` gesture guard is owned
  by the integrator. Native Obsidian/AT/user-study checks remain I16/I17 work.
- Preserved command/history/geometry/state boundaries: Plan still renders handles and activates
  geometry actions; Renovate still preserves selection and camera, and its arrow nudge creates
  no command/history entry. No schema or command-history shape changed. Existing perspective
  routing remains responsible for draft confirmation, saving/busy refusal and Review-return
  snapshot restoration.
- Remaining risks, conditional decisions and release impact: do not merge/release I14 as a
  complete accidental-edit guard until the integration request lands and pointer boundaries are
  verified. Without it, hidden handles alone do not prevent drag-driven geometry edits or
  dimension-button mutation.
