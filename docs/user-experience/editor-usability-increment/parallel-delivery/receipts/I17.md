# I17 receipt — Align help and prepare novice acceptance evidence

- Packet and outcome: I17. Updated the user help with dated current-route amendments and
  prepared a recruitment-ready novice/accessibility protocol with separate first-use,
  mode-comprehension and secondary-task scorecards. No production code, locale, CSS or tests
  were changed.
- Assigned model/reasoning: delegated coordinator session / high.
- Status: review; documentation and scorecard preparation complete. Human, native Obsidian and
  assistive-technology evidence remains unperformed.
- Prerequisite PRs and confirmed merge SHAs: final I16 head `2e6c82cb8` is integrated by merge
  `3aa863df2`; it records the complete green repository gate and the unavailable native/AT environment. I15/I18 evidence limits remain explicit in that ancestry.
- Base SHA: `2e6c82cb8` (`codex/usability-i16-integration-native`).
- Branch/worktree: `codex/usability-i17-docs-user-validation` /
  `D:\Projects\renovation-planner\.worktrees\usability-i17-docs-user-validation`.
- PR URL and current commit: pending coordinator creation; the branch tip includes final I16 serialization.
- Owned files changed: `docs/using-plan-editor.md`, this receipt, and
  `docs/user-experience/editor-usability-increment/user-validation-protocol.md`. The protocol
  is the requested recruitment/scorecard documentation under the existing usability package.
- Shared-file integration request and resolution: none. No production or shared root/locale/CSS
  file was needed.

## Documentation changes

- The help now uses the current visible start labels **Add rooms**, **Upload a floor plan** and
  **Start empty**.
- It distinguishes **Plan**, **Renovate** and **Review**, names **Edit geometry in plan** as
  the Renovate exit to geometry editing, and preserves the current no-write perspective switch.
- It states the I18 limitation precisely: existing arbitrary Room/Area corner editing is
  pointer-drag only; no approved non-drag/keyboard route exists. Numeric coordinates are not
  presented as a substitute.
- The protocol contains screening quotas, neutral moderator wording, assistance levels,
  first-use/mode/secondary denominators, AT/native evidence cards, and explicit blank/not-run
  fields. It preserves the product-owner release-decision dependency and does not synthesize
  participant data.

## Commands run and exact results

- PowerShell Markdown relative-link check over the three changed documents — passed, 7 links
  checked; all local targets exist.
- PowerShell source wording assertions — passed, 13 checks for current EN/DE locale and
  component strings: `Add rooms`, `Upload a floor plan`, `Start empty`, `Edit geometry in plan`,
  `Plan`, `Renovate`/`Renovieren`, `Review`/`Prüfen`, `Fit floor`, `Fit selection`, `Details`,
  `Close panel`, and the Plan-only geometry guard. Stale-claim checks also passed.
- `git diff --check` — passed after the documentation edits.
- Application tests — not run and not claimed; this is a prose-only change. I16 owns the final
  `npm run check` and native release receipt.
- Final-parent verification: `git diff --check` and the same seven-link/source-wording checks remain green after integrating I16. I16's `npm run check` passed 905 files / 9,959 tests with one intentional skip, coverage above thresholds and a clean Fallow result.

## Evidence and limits

- Browser evidence used for wording/context: I15's connected-harness observations B01–B09 at
  the route `?view=plan-editor&reference&planning&fidelity`, including dark English, German
  460 px and 900/899 plus 400/399 boundaries. These are not new I17 participant results and no
  screenshot/native/AT claim is made.
- Native/AT/user-study checks actually performed: none.
- Unperformed checks and why: no human sessions were scheduled in this delegated documentation
  task. Native Obsidian, real vault persistence, screen reader, host zoom, contrast/target-size
  measurement and participant learnability remain named release work.
- Preserved command/history/geometry/state boundaries: documentation only; no runtime behavior,
  command granularity, geometry, selection, draft, host or locale behavior changed.
- Remaining risks, conditional decisions and release impact: I18's arbitrary-corner
  non-drag/keyboard criterion remains explicitly unsupported; I15's saved screenshot evidence
  limitation remains open; separate first-use, mode-comprehension, secondary and AT/native
  counts are all blank/not-run. A product owner must decide release only after those evidence
  streams are completed or scope is explicitly narrowed.
- Reviewer findings, follow-up SHA and addressed threads: none.
- Confirmed merge SHA and cleanup result: not merged; provisional branch is to be pushed and
  retained for coordinator serialization.
