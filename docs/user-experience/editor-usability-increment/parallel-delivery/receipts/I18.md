# I18 — Conditional non-drag corner access disposition

- Packet and outcome: explicitly deferred in accordance with I00. No corner-editing interaction is approved, so I18 adds no production control, shortcut, selector, geometry route or schema and does not restore the rejected coordinate form.
- Assigned model/reasoning: coordinator (`gpt-5.6-sol`) / high.
- Status: reviewed-deferred; documentation and unchanged-boundary verification are complete, while the user-facing capability remains unsupported.
- Prerequisites and decision source: I00 PR #174 records the conditional deferment; I10 PR #188 and I14 PR #193 are present through the stacked base.
- Base SHA: `6cf16e82a` (`codex/usability-i14-mode-input-guard`).
- Branch/worktree: `codex/usability-i18-corner-access` / `D:\Projects\renovation-planner\.worktrees\usability-i18-corner-access`.
- PR URL and current commit: pending creation; the branch tip will include this durable record.
- Files changed: this receipt and `docs/issues/Vertex editing has no keyboard path.md`. `select-tool.ts`, `InteractionLayer.vue`, geometry commands and tests are intentionally unchanged because no interaction is approved.

## Exact unsupported criterion and release consequence

The unsupported task is selecting one arbitrary Room/Area vertex, changing only that vertex without a drag, reviewing the same geometry impact, and committing through the existing reversible geometry command. Whole-selection arrow nudge is not vertex editing; Plan pointer dragging is not a non-drag path; the rejected numeric coordinate form is not an approved substitute.

This increment therefore cannot claim that corner editing meets the non-drag/keyboard criterion, cannot cite it as WCAG-conformant or assistive-technology validated, and cannot close the issue. I15 and I17 must retain the limitation in their reports; I16 must distinguish automated pointer/keyboard regressions from native human/AT evidence.

## Verification and preserved boundaries

- Unchanged-boundary gate: `npm run check:fast -- tests/presentation/editor/inputInteractions.test.ts tests/presentation/editor/resize/outlineProposal.test.ts` passed with `oxlint`, `vue-tsc`, 2 files and all 11 tests green in 59.29 s. `git diff --check` also passed.
- No browser/native/AT/user test is claimed; performing those without an approved interaction could only reconfirm the known gap.
- Existing Plan pointer vertex dragging, whole-selection nudge, geometry validation, preview, command granularity and Undo/Redo remain unchanged. I14's Renovate mutation guard remains unchanged.
- Confirmed merge SHA and cleanup result: not merged; keep the worktree until the stacked PR is reviewed.
