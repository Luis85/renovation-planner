# Packet receipt

- Packet and outcome:
- Assigned model/reasoning:
- Status: not started / implementing / review / merged / conditional-deferred
- Prerequisite PRs and confirmed merge SHAs:
- Base SHA:
- Branch/worktree:
- PR URL and current commit:
- Owned files changed; any explicit ownership transfer:
- Shared-file integration request and resolution:
- Commands run and exact results (include no-match/skipped limits):
- Browser evidence paths, fixture/URL, viewport, theme, locale and input method:
- Native/AT/user-study checks actually performed:
- Unperformed checks and why:
- Preserved command/history/geometry/state boundaries:
- Remaining risks, conditional decisions and release impact:
- Reviewer findings, follow-up SHA and addressed threads:
- Confirmed merge SHA and cleanup result:

Do not populate results from plans, prior receipts, generated mockups or another commit's run. A conditional deferral records a limitation; it is not a passing implementation or accessibility result.
