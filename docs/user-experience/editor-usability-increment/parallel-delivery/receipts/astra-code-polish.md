# Astra corrective code review

- Date: 2026-09-14. Dedicated GPT-6 Astra/high corrective pass; no redesign or new domain feature.
- Branch: `codex/usability-astra-code-polish` in `D:\codex-worktrees\1b59\renovation-planner`.
- Stacked base: `codex/usability-i17-docs-user-validation`, refreshed to `d211d75ee44402486f99561e895395ea9decc981`, including main `fa78293a9`. The refresh fast-forwarded without conflicts and retained the corrections in progress. The main checkout was not edited.
- Status: ready for stacked review; corrective focused checks and final build/lint/analyze passed. The combined full gate remains outstanding. No PR is merged by this pass.

## Findings and corrections

1. **Plan drag survived Renovate.** I14 guarded starts but its receipt overstated cancellation. Select remains active across perspectives, so returning to Select does not run the outgoing-tool lifecycle. A synchronous registration watcher now retires an active geometry gesture on leaving Plan and clears ToolManager's interrupted gesture flag. Select also rechecks permission and the stale-write gate before pointer continuation and release. Cancellation covers room body/corner, wall end, element, group, label and rotation state. Marquee selection is retained. A completed dispatch retains its owner and preview until settlement.
2. **Menu and measurement actions bypassed the mode boundary.** Add point and geometry edit/create commands now expose disabled availability outside Plan and recheck current permission even when a caller retains a previously generated menu action. Wall/opening edit, bulk measurements, move-to-point and wall splitting are guarded at their runtime admission and post-read/dispatch boundaries. The direct wall-length control exposes the same refusal. Independent review also found Enclose with walls and group and room/wall group transforms still admitted geometry in Renovate: those entrances are now Plan-only, with a final group dispatch check distinguishing membership changes from geometry changes. Existing element action guards remain authoritative. Room metadata, group/ungroup, renovation records, selection, camera, and intentional clipboard behavior retain their existing routes; structure removal retains its prior separate admission rule.
3. **Handoff focus was lost with the outgoing component.** Renovate opening and Edit geometry in plan capture the persistent inspector before navigation. After Vue patches, they focus that surviving visible region only if the opener disappeared. Refused/busy routes preserve the opener or active dialog focus.
4. **Durable status contradicted GitHub.** I15/I16 no longer claim that no PR was opened, and I17 records its refreshed published head. PRs #195, #198 and #199 were verified OPEN and MERGEABLE on 2026-09-14. The delivery index distinguishes historical packet/dispatch planning status from current receipts. User help no longer claims layout Add remains available in Renovate; it names Select/Pan and the eligible Add work item route.
5. **Late combined-UI finding: the wrapping taskbar covered Add detail.** The accepted 460x800 EN-light and DE-dark screenshots on UI branch `e2a1ee8fb19f5eb8e7a96ad2e0846ffed75c37ca` show the selected-room popover extending into the two-row taskbar. Reused `useTaskbarClearance` with opt-in content-height measurement, observing the popover as well as the canvas and taskbar. Its anchor now preserves the existing minimum allowance while also fitting above the actual taskbar. New tests use the supplied EN/DE canvas measurements, then grow the taskbar and popover and assert separation, focus, camera and geometry/version retention. The separate UI branch's CSS is not imported here.

## Reconciliation review

Reviewed the I17 stack relative to current main, including the production UI, localization, styles, tests and delivery records. Inspected current-main seams for item rectangle/free-outline tasks, asset shape resolution, posts/beams, label/rotation/element movement, and edge scrolling. The existing asset-shape candidate provider, task instructions, structural metadata and movement commands remain in place. No additional validated reconciliation defect was found in these seams. Asset-symbol PR #197 enters through the refreshed I17 base, not a separate main merge.

The new gesture cancellation makes `activeToolTracksPointer()` false immediately for canceled geometry; edge-scroll tests remain part of the focused gate. Full repository verification also includes the current-main asset and structural-element suites. This review does not substitute for native Obsidian or assistive-technology validation.

## Verification

- `npm ci --ignore-scripts`: installed the locked 581-package dependency tree; no tracked dependency changes.
- New regression `npm run check:fast` over `i14-perspective-transition`, `i14-select-permission`, `i14-context-menu-guard`, and `i11-opening-details`: 4 files, 20 tests passed in 49.55 s, with oxlint and vue-tsc passing.
- Broader `npm run check:fast`: 31 files, 191 tests passed in 153.44 s. Exact targets: all `tests/presentation/editor/usability`, plus `contextMenuActions`, `contextMenuLifecycle`, `renovationWorkflow`, `renovationRetiredActions`, `inputInteractions`, `escapeRouting`, `structureSelection`, `openingMove`, `shell/responsiveShell`, `tools/selectTool`, `tools/selectToolDropPreview`, `tools/selectToolLabelDrag`, `canvasEdgeScroll`, `surface/edgeScroll`, and `elementMoveAdmission` under `tests/presentation/editor/` (each named suite has the `.test.ts` suffix).
- Initial verification feedback: two new busy-path tests tried to assign readonly save state; corrected them to drive `beginSaving`/`resolveNeutral`. Targeted ESLint requested two function-size reductions; extracted the menu guard and consolidated existing store declarations. No gate was weakened.
- The first full check passed build/lint, then was deliberately interrupted during coverage when independent review validated the additional enclosure/group-transform bypass. No full pass is claimed for that attempt.
- Read-only `npm audit --json` reported 2 high findings: development dependencies `fast-uri` 3.1.5 and `js-yaml` 4.3.1. No audit fix or tracked dependency change was applied.
- Group/menu corrective `check:fast`: 5 files / 45 tests passed in 44.97 s (`i14-group-geometry-guard`, `i14-context-menu-guard`, `groupEditing`, `groupNativeActions`, `groupRotationDispatchBoundary`). Targeted ESLint and `git diff --check` then passed. The single Impeccable detector over the three changed Vue guard/handoff files returned `[]`; it preceded the coordinator's late positioning request and was not repeated.
- Complete `npm run check` on the reconciled code: build transformed 1,383 modules and full ESLint/oxlint passed. Coverage run completed in 1,307.19 s: **932 files passed / 2 failed (934)**; **10,218 tests passed / 2 failed / 1 skipped (10,221)**. Coverage: statements 99.23% (25,815/26,014), branches 98.09% (19,039/19,409), functions 99.28% (7,460/7,514), lines 99.67% (19,094/19,156). Exit 1 means this is not a full green gate, and the chained analyze step did not run in that attempt.
- Full-run corrections: `wallRotationRuntime` still expected a successful geometry edit in Renovate; it now explicitly refuses Renovate and exercises successful rotation/readback recovery in Plan. `groupAdmissionCoverage` caught lost disabled state for object-bearing groups when transform targets were hidden; the underlying admission target and visible editable target are now separate, retaining that pre-existing restriction. The direct wall-length integration case likewise explicitly uses Plan for its geometry edit.
- Final corrective `npm run check:fast`: **25 files / 147 tests passed in 69.67 s**, with oxlint and vue-tsc passing. Exact targets: all `tests/presentation/editor/usability`, plus `directCanvasActions`, `wallRotationRuntime`, `groupAdmissionCoverage`, `groupEditing`, `groupNativeActions`, `shell/responsiveShell`, and `shell/temporaryToolBanner` under `tests/presentation/editor/` (each named suite has the `.test.ts` suffix). Both reported full-run failures pass in this corrected run, as do the late popover tests.
- Final `npm run build`: passed, 1,383 modules transformed; Vite build 3.93 s. Targeted `npx eslint` over every changed TS/Vue file and new test passed with `--max-warnings 0`. `npm run analyze`: exit 0, 1,072 files analyzed, no dead code or duplication, 0 complexity findings above threshold. Its existing informational warning about the hidden `.claude` directory is not suppressed. `git diff --check` passed. Node was v24.20.0 on Windows.
- Per coordinator direction, the late positioning correction gets proportionate focused verification here; the final combined UI branch owns the next full gate and combined screenshot confirmation. No final all-green full-gate claim is made by this receipt.
- Markdown links: 7 changed documents / 29 local links checked, all resolve.

## Live browser evidence

Used the Codex in-app Browser at `http://127.0.0.1:5193/?view=plan-editor&reference&planning&fidelity&select=harness-kitchen`, dark English. The supplied route initially rendered an empty floor. Through normal UI actions, created a disposable Kitchen (4 m by 3 m), enclosed it with walls, selected Wall 1, and added a hosted Door through the context menu.

- Desktop 1280x720: keyboard Renovate: Door changed the perspective and left the accessibility tree's focused element on Inspector. The room handoff and Edit geometry in plan also returned focus to Inspector.
- Narrow 460x900: the open Details drawer survived both keyboard handoffs for the Room and hosted Door. The focused element remained the visible Inspector rather than document.body. Inspected rendered screenshots of the Plan Room and Renovate Door states. Restored the browser viewport afterward.
- Automated tests additionally cover both opening handoffs at 1200px/460px, busy refusal, an existing confirmation dialog, geometry/version/history equality, exact selection retention, pending structure reads, retained Plan menu callbacks, and immediate gesture/edge-scroll retirement.
- Screenshots were inspected inline; no saved screenshot artifact or contrast/target-size measurement is claimed. Busy/refused browser journeys were covered by the automated integration tests, not native host tests.

## Remaining risks and release limits

Native Obsidian persistence/reopen, split leaves, PDF loading, host themes/zoom, screen-reader behavior and participant validation remain unperformed. I18's arbitrary-corner non-drag/keyboard route remains explicitly unsupported. I15's saved screenshot evidence limitation remains open. No WCAG conformance or release-readiness claim follows from these automated gates. The separate UI-fidelity pass is not part of this branch and needs verification after the coordinator integrates it.
