## Opening edits with exact preview and consistent entry points

Selected doors and windows expose Width, Along wall and Swing buttons, a compact menu,
keyboard/right-click actions and an Inspector entry into the same DOM preview panel.
Width changes preserve the current centre; offset translates along the host. Exact fractional
mm survive numeric entry and 10 mm steps. Invalid containment/overlap proposals explain refusal.
Apply is one reversible Structure command; no-op and Cancel add no history.

Initial Plan admission is guarded around asynchronous version reads. Selection, host, tool,
perspective departure and unmount retire the draft. Read-back retry after a successful write
cannot dispatch again. Existing pointer move clamping and wall-side/color persistence remain.

The form floats where 420 px clear height is available, otherwise docks below the canvas.
Below the existing 400 px canvas minimum, an already-selected opening keeps a nonspatial
numeric fallback alongside the widen-pane guidance. Canvas-boundary changes cancel drafts.

## Verification

- 132 tests passed across the entire R01 manifest plus responsive-shell regressions.
- Five keyboard/context cases passed after the final menu clearance correction.
- Type-check, targeted ESLint, targeted Oxlint and package schema/link validation passed.
- Actual in-app browser evidence: EN/DE in both themes; 1280, 460, 400 and 320 px;
  exact steps, Apply/Undo/Redo, compact/context/Inspector focus and 44 px controls.
- Explicit 200% CSS magnification fixture passed the interaction check. Native browser zoom,
  native Obsidian, screen-reader and human acceptance are not claimed.
- The focused coverage diagnostic failed unchanged thresholds and informed the additional
  boundary tests; definitive full coverage and Fallow run in this PR's CI.

Receipt and original JPEG captures/hash ledger:
`docs/user-experience/remplanner-adoption/parallel-delivery/receipts/R01.md`,
`docs/user-experience/remplanner-adoption/evidence/r01/`.

Stack base: #215, `codex/remplanner-adoption-plan`. I18 remains open. No schema migration.
