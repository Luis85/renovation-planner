# Session and delivery contract

The parent owns stack placement, coding admission and verification. Current instruction: **no dispatch and no npm/Node/build/test/preview while wall verification owns the machine**. Static research, documents and Git publication may continue. JSON/link validation waits for release.

| Owner | Task ID / branch | Reservation |
|---|---|---|
| Parent | `01a09aba-b390-7650-a5d0-8d62f3066d29` | Strict stack, leases and launch authority. |
| Wall | `01a0a05e-1ed9-7541-969c-4d71e6ee7cee` / `codex/usability-astra-wall-tool-polish` | Walls, drawing/thickness, Structure command admission, shared opening seams; full verification. |
| Color | `01a0a060-b9d8-70c2-892f-ce30ba026b08` / `codex/usability-astra-item-colors` | Item/asset color, palette, persistence, menus/Inspector, styles/locales. |
| Plan | `01a0a064-04e7-7252-becf-c54cdc07819d` / `codex/remplanner-adoption-plan` | This package; zero new coding workers. |

Wall's 2026-09-14 message explicitly left compact opening UI to follow-up and identified the action-level Renovate gap. This confirms the gap but does not release shared files. Parent separately forbids dispatch until later instruction. Broad manifest prefixes are conservative locks, not permission to modify everything inside. Exact packet paths are future allowlists after release. New helper paths need an ownership amendment. `targetedTests` are read/run regression targets; workers edit their new test files by default and obtain coordinator ownership before changing an existing test.

## Launch and supervise

1. Read project instructions, final wall/color receipts, [contracts](../implementation-plan.md), [validation](../validation-plan.md) and [manifest](dispatch.json). Inspect current tasks, PRs and branches to avoid duplicates; prefer compact app task snapshots.
2. Obtain parent-supplied immutable stack tip and release. Run package validation. R00 records owners/leases and reconciles paths. At most three coding tasks across the whole program, including wall/color. A free slot never overrides a path conflict or no-dispatch instruction.
3. List app projects; choose the actual Renovation Planner project. Create a fresh Codex task with exact assigned model/high, complete prompt and worktree at the verified parent ref. Worker must verify HEAD equals supplied SHA before editing. Record returned real task ID; pending client ID remains separate until resolved.
4. Worker creates one codex topic branch in its assigned worktree. No main/lower-stack edits. Include related docs, EN/DE, contracts and receipt. Targeted checks only under the lease; one named owner alone runs full gates.
5. Commit imperatively and push. If strict parent is not ready, wait before PR creation. Parent coordinates one PR against the immediately preceding concern, never a sibling against main. No merges.
6. Supervise with bounded `wait_threads`; inspect receipts and arrange routine fixes on existing worker branches. For review feedback, fetch comments/threads, patch, verify, push, reply with SHA/checks, then resolve. Never claim a thread addressed before a pushed fix.
7. R04 acquires the heavy lease, runs combined verification and records native/human limits. Parent/human decides release. Cleanup follows confirmed merge, fetch/prune and main fast-forward, never unconfirmed force deletion.

## Model and state discipline

Luna/high owns R02's settled disclosure/copy/tests. Terra/high owns R01/R03's geometry or persistence boundaries. Astra/high owns reconciliation/integration/review; do not rewrite satisfactory code for preference alone. Contract conflicts go to the parent with exact file/SHA/evidence and a bounded decision, while independent permitted work continues.

Only coordinator edits shared manifest. Workers own receipts/Rxx.md and their evidence. Record dependency SHA, task ID, branch, pushed SHA, status, paths, blockers and verification owner. Pushed is not accepted. Null dependency SHAs deliberately prevent launch. Fill [prompts](prompts.md) from admitted state before task creation; never dispatch placeholders.
