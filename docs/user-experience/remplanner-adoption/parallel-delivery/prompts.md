# Copy-ready packet prompts

Prefix each packet with the common envelope. Fill angle-bracket fields from R00's admitted state; **never launch unresolved fields**. Set the app model/reasoning explicitly. Current status: no task creation permitted.

## Common envelope

```text
Work on Renovation Planner in the fresh assigned Codex worktree. Use <MODEL>/high. Read AGENTS.md if present, .codex/instructions.md and applicable workflows if present, CLAUDE.md and relevant SDD sections.
Verified parent <PARENT_REF> at <PARENT_SHA_40>. Reviewed planning package commit <PLAN_SHA_40>, docs/user-experience/remplanner-adoption/. Verify HEAD/ancestry and create branch <BRANCH>. Read your packet in implementation-plan.md, validation-plan.md and parallel-delivery/README.md. If parent differs, stop dependent edits; do not choose another base. Parent task: 01a09aba-b390-7650-a5d0-8d62f3066d29.
Own only manifest paths and your new tests/receipt/evidence. Respect reservations; no main/lower-branch edits, Add detail restoration, I18 closure, changed mode semantics, copied RemPlanner assets/code/identity or new persistence truth. Use native Obsidian UI and EN/DE.
Apply the approved selected-item task-button and compact overlay/right-click-menu principle in R01/R03: same reversible actions and shared commands, Inspector/keyboard parity, visible focus, 44px targets and responsive access; no canvas-only capability. The door/window image is R01's primary design input; follow explicit local semantics rather than guessing unlabeled arrows. Read the updated packet acceptance clauses before implementation.
Lease: <OWNER_AND_ALLOWED_CHECKS>. Run admitted targeted checks only, no full check/coverage/build/Fallow without lease. Record browser/native/AT/human evidence honestly. Commit imperatively, push one concern branch and write receipts/<PACKET_ID>.md with base/pushed SHA, paths, exact checks and blockers. Wait for coordinator strict-stack placement before PR creation. Never merge. Send routine blockers to parent, not the user.
```

## R00 — gpt-6-astra/high — codex/remplanner-r00

```text
Reconcile parent-supplied final wall→color tip with this package. Inspect receipts, schema and StructureCommand admission/cancellation. Revalidate remaining opening gap and all paths. Record immutable launch parent, allowlists and leases in dispatch.json; update contracts/prompts only for evidence-backed drift. Run package validation after release. Deliver R00 receipt without speculative UI. Do not dispatch until parent lifts dispatchAllowed=false.
```

## R01 — gpt-5.6-terra/high — codex/remplanner-r01

```text
Implement only compact selected-door/window size and swing controls left by wall owner. Follow R01's centre-anchored width, along-host offset, visible 10 mm steps plus exact input, labeled hinge/side/angle and Preview→Apply/Cancel contract. Reuse Structure command and final initial-write admission, containment/overlap validation. No endpoint drag-resize or new schema. Preserve eligibility, IDs, metadata and wall/color data. Close openingMove's action-level Renovate gap while preserving undo/redo. Handle canceled delayed reads, stale/deleted hosts and read-back retry without duplicate dispatch. Provide the same keyboard/list/context/Details DOM route, focus and 44px targets. Run manifest regressions plus new behavioral tests under lease. Report allowlist changes before editing.
```

## R02 — gpt-5.6-luna/high — codex/remplanner-r02

```text
Add Drawing and keyboard help as native disclosure inside View with EditorDrawingHelp component. Explain settled routes from current handlers, pointer alternatives, text-field/reference-preview scope, modes and I18 limitation. No new shortcut/global listener/modal framework/tutorial overlay/persistence. Opening/closing help preserves task/draft/selection/camera/history and existing Escape return behavior. Add concise EN/DE, scoped host styling if needed and focused interaction/copy tests. Recheck final opening/wall/color terminology; never restore Add detail. Capture desktop/narrow/light/dark evidence only with browser lease.
```

## R03 — gpt-5.6-terra/high — codex/remplanner-r03

```text
Implement Selection measurements in View, default on, for passive selected Room/wall labels only. Keep authored Measurements, active draft/preview/inline dimensions and every other WallCanvasActions action. Add optional selectionMeasurementsVisible to device-local preferences with absent/invalid→true, changed-key merge, existing per-leaf snapshots and failure logging. Update interface/hydration/watch/reset; no Markdown/geometry schema writes. Reconcile final wall labels first. Keep checkbox truthful during gestures; never hide focused input. Test reopen/two-leaf merge, passive hidden/active visible, unchanged geometry/history, correct obstacles and restored label entry. Use manifest targeted regressions and new tests; R04 owns full gate.
```

## R04 — gpt-6-astra/high — codex/remplanner-r04

```text
Review completed R01–R03 strict chain and wall/color integration. Confirm receipts and fix concrete defects through owning branches. Acquire one heavy lease, run npm run check unchanged, then validation matrix in real harness and Obsidian where available. Use Impeccable detector, one batched visual inspection and at most one confirmation pass. Record source identities, captures/hashes, persistence/recovery and keyboard evidence; native/AT/human checks remain unperformed if unavailable and I18 unsupported. Update docs/manifest/receipt, push and coordinate PR placement; never merge.
```

## R05 — gpt-6-astra/high — codex/remplanner-r05

```text
After immediate-wave acceptance and selection of this later study, design local handoff/print contract only. Inspect Review-note generation and proposed professional-handoff journey. Specify purpose, snapshot/inclusions, Existing/Planned identity, scale/calibration, pages/units/fonts, curve fidelity, non-color legend, text companion, stale/unknown treatment and local infrastructure boundary. Propose separately dispatchable implementation and output validation. No Export button/PDF code/schema/professional approval claims/catalogue. Record unresolved choices for selection.
```
