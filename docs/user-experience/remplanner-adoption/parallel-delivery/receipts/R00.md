# R00 — reconcile delivered wall/color stack

Date: 2026-09-15. GPT-6 Astra/high, adoption coordinator task `01a0a064-04e7-7252-becf-c54cdc07819d`, branch `codex/remplanner-adoption-plan`.

Normally merged final #214 color head `b5c4ccda08d98e0e966941250de90544948d2e62` into the planning branch at merge commit `3f76fc964`. The imported tip includes #212, #213 and current main `9ef6070302cdd92a867668236a434c02d613f2fb`; `git merge-base --is-ancestor origin/main HEAD` exited 0. No top-only main merge is needed at this point. Lower PRs/branches and main are untouched.

Verified #214's head/base and all six successful CI checks. Its base is `codex/usability-astra-wall-side-thickness`. The adoption-plan PR must be based directly on `codex/usability-astra-item-colors`, with no sibling PRs or merge to main.

Reconciliation: independent A/B wall depths (schema 13), clipped opening frames/masks and host reach are delivered and must survive R01. Item/asset colors use schema 14, not the earlier proposed 13; default plus six named presets share the menu/Inspector action. Preserve `StructureCommand` initial admission around asynchronous version reads while leaving successful undo/redo independent of the retired preview. `openingMove.ts` still has its action-level Renovate admission gap, so R01 retains that targeted correction.

The selected-item task-button and compact overlay/right-click-menu principle is approved. R01's primary door/window image drives local width/along-host offset/hinge/swing controls with shared reversible commands, Plan-only preview/Apply/Cancel, Inspector/keyboard parity, 44px targets and responsive fallback. Independent A/B edits are no longer rejected scope. Room independence, official-source contradiction caveat, Add detail removal and unresolved I18 remain binding.

Initial package validation failed correctly on five current-run JPEG captures named `.png`; renamed to `.jpg` without changing bytes. Installed isolated temporary jsonschema/Pillow validation dependencies, with no package/lockfile change. Complete structural and Draft 2020-12 JSON Schema validation, current path checks, local links, image decoding and SHA-256 now pass; exact counters are in [validation-result.json](../validation-result.json), hashes in [evidence-hashes.json](../evidence-hashes.json). Publication whitespace checks apply to the final staged diff. No production UI change or new full local code gate is claimed; imported source has #214's green gate and plan PR CI is the next admission barrier.

Coding stays queued until that PR is green. Coordinator owns one exclusive heavy lease; workers are serial R01 → R02 → R03, each with targeted tests and green stacked PR, then R04 Astra/high performs UI fidelity, code improvement, current-main top refresh/merge polish and real-vault review when native computer tools permit. R05 remains deferred. Parent's prior runtime prohibition is superseded by its autonomous-resume instruction.
