# R01 — Direct opening size and swing

- Task: `01a0a433-a8e1-7213-8054-25c806b53729`
- Branch: `codex/remplanner-r01`
- Immutable base: `bb149cd4a2f38936be9718d511b7d90164cb1847`, planning PR #215.
- PR: [#216](https://github.com/Luis85/renovation-planner/pull/216), directly above `codex/remplanner-adoption-plan`.
- Initial Terra/high implementation: `4523dc68ee3b0fb49195f6b093bfd49c6a7f9566`; initial published tip `d48d925732ce739b02cb1be1ef5223ddd201f129`.
- Astra/high review and remediation: this follow-up, authorized by coordinator after incomplete initial verification.
- Status: verification completed locally; awaiting the follow-up CI result and coordinator acceptance.

## Behavior and review corrections

Selected doors/windows expose Width, Along wall and Swing task buttons, a compact disclosure,
keyboard/right-click actions and Inspector entry into one DOM panel/state/StructureCommand.
Existing height/sill and plain-opening measurement routes remain available.

Raw fractional mm survive exact entry and 10 mm steps. Every width edit preserves the current
preview centre, including after offset edits and invalid intermediate text. Untouched absent
swing stays absent. Invalid, overlapping and out-of-host proposals explain refusal; the existing
pointer move still clamps. Initial action admission and the asynchronous initial command write
require Plan. Cancellation, selection/host/tool/perspective departure (including away and back)
and disposal retire callbacks. Successful writes with failed read-back close the editor;
refresh retries never dispatch again.

Astra corrected the initial implementation's lost callback opener, unguarded move initial write,
invalid-text centre loss, missing host retirement, lost plain-opening/height/sill entry, and
under-sized Inspector/context targets. One panel instance mounts at a time. The floating panel
requires 420 px clear height beside the complete leaf/swing. Otherwise it docks below the canvas
and frames the opening above the taskbar. Below the unchanged 400 px canvas floor, an already
selected opening has a nonspatial editor alongside the existing widen guidance. Crossing that
boundary cancels its draft and preserves selection/camera. This is not mobile canvas parity.

## Verification

- Complete R01 manifest plus responsive-shell regression: **11 files, 132 tests passed**,
  single worker; 2026-09-15 11:51:19 local start, 129.49 seconds, exit 0.
- After the context-menu target/clearance correction: **5 targeted keyboard/context tests
  passed**, 29 unrelated cases skipped by the name filter, exit 0.
- `node node_modules/vue-tsc/bin/vue-tsc.js --noEmit`: exit 0.
- Targeted ESLint on changed Vue/TypeScript modules, fixture and R01 tests: exit 0,
  zero warnings. Final context-menu subset also exit 0.
- Package checker: JSON/schema, 74 owned paths, 41 links and reference-image hashes passed;
  `git diff --check` passed.
- The one authorized focused coverage diagnostic ran **22 passing tests**, but exited 1
  against unchanged 99/99/99/98 thresholds: statements 90.09%, functions 83.90%, lines
  92.36%, branches 86.49%. It identified missing DOM action/read-failure/retirement coverage;
  subsequent tests expand R01 to 34 cases. This is not a full-gate result.
- Terra's earlier Impeccable detector returned `[]` before Astra's responsive corrections.
  No second detector run or final detector result is claimed; R04 owns the combined review.
- Initial PR CI failed on actual lint complexity/format findings; those were fixed without
  changing any threshold. Final CI status is recorded in the PR after this follow-up.

## Browser evidence

[Evidence and capture notes](../../evidence/r01/review.md) accompany actual JPEG screenshots
and hashes. The writable reference/planning harness seeds a door and curved-host window through
the real sidecar repository, with asymmetric A/B extents. Browser actions verified numeric
sequence, Apply/Undo/Redo, Inspector and context focus, refusal and responsive fallback.
The initial read-only-harness limitation in the first receipt was incorrect and is withdrawn.

## Limits

No schema, wall geometry/clipping or item-color module was changed. Native Obsidian,
screen-reader/AT and human acceptance remain unperformed and belong to R04. I18 remains open.
200% evidence uses explicit CSS magnification with a 460 × 400 CSS-pixel embedded viewport;
the in-app browser did not change its native zoom through keyboard shortcuts.
