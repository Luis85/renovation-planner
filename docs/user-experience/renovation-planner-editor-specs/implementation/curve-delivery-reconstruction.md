# Curve delivery reconstruction

Current follow-up: [PR #111 implementation and automated verification](curve-delivery.md).
Manual acceptance is user-owned and pending. The text below is the historical
reconstruction receipt, not the current verification status.

Historical status: source reconstructed; all verification pending on that delivery tree. No
test, type, lint, build or browser execution was performed during reconstruction.
Historical receipts in curved-boundaries.md remain evidence of their original trees,
not proof of this new concern composition or rendered-screen acceptance.

Base: `codex/editor-deliver-group-storage` at
`858f3735dcd3954d6d39157a0fcd59629160db84`, including independent Photo/Reference
delivery. This branch adds schema 7 after Group 6; it does not introduce Stair 8.

| Original | Reconstructed | Scope |
| --- | --- | --- |
| `3192c463` | `3bafafe9` | Analytic circular geometry, version 7 storage/migration, Room/Wall fields, grouped curved validation and Zone receipts, original foundation tests. |
| `6dcb8942` | `7ca510fa` | Explicit curve task, native precise controls and bend handles, rendering, hit tests, measurements, host-opening math and curve-preserving transforms. |
| `af7583b0` | `4f2c6961` | Curved quantities, normalized evidence-pin decoding, snapping, lifetime cancellation and strict idle-scene fixes; associated tests and historical receipt. |
| Curve portions of `2c872609` | `0b585e63` | Circular contacts, collapsed arc math, draft/selection geometry, native recovery tests, and the peer curve-only rotation baseline refusal. |
| Curve portions of `46f30207` | `0b585e63` | Tangency discriminant tolerance, paused draft retention, honest runtime type, private validation helpers and derived-radius tolerance. |
| Curve portion of `408c7dea` | `0b585e63` | Deferred read gate correction in recovery tests. |
| Curve portion of `f5a28f45` | `0b585e63` | Consistent German formal instruction. |

Shared source resolutions are applied without importing integration merge ancestry:

- Keep visibility-filtered canvas candidates and forward their optional bulges.
- Keep the published rotation runtime argument type; compose CurveTaskRuntime beside it.
- Use the final array-based ZoneLayer preview adapter, passing the curve document's
  objects. RoomDimensionLabels receives the complete curve preview document.
- Preserve existing input/marquee/hover admission and all previous locale modules.
- Leave Group UI preview fallback, group facade fields and Group snapshot extensions
  to the later Group UI concern. Group storage already preserves member geometry.

Deferred from `af7583b0`: editor-curves-check.mjs, editorCurveProbe.ts and the Curve
accessor in editorFidelityProbe.ts. Those browser artifacts depend on the later Group
UI probe and belong to cumulative verification. Their later rotation/Wall journey
extensions are likewise deferred. No browser execution is claimed here.

Deferred from `2c872609`/`46f30207`: generic rotation hover admission, disposed baseline
read, kind-only change and compensation-aware write-refusal tests. Those belong to
rotation/combined verification; only the curve-metadata refusal is extracted here.
Mixed coverage and repair receipts are replaced by this concern-specific mapping.

The storage refusal fixture now uses schema 8 as the first unsupported value, because
7 is supported here. This is a Curve7 acceptance adjustment, not the later Stair8
dynamic-version fixture or schema implementation.

Static comparison found the three repaired production files (CurvedPolygon,
circularIntersections and curveTask) and the contacts/draft/recovery test files
identical to `46f30207`. Shared renderer/runtime/DTO files require cumulative comparison
after Group UI, Opening movement and Stair owners apply their independent extensions.
No thresholds, assertion timeouts, skips, lint rules or test exclusions were changed.
