# Acceptance delivery reconstruction

Base and PR base: `codex/editor-deliver-quality`, source `4e098a02`.
Reconstructed acceptance commit: `10fbc9c70e13e075fe3e8fd5a38171a0330636df`.
Frozen comparison target: `b10c3b24d51735fdf53b3acd5fbc095024c987ad`.

Status: reconstructed, unrun on this branch. No tests, linters, type checks, builds,
captures or native actions were executed during this transfer. No production `src`
or `styles` changes, timeout changes, skipped tests or threshold changes were made.

## Reviewed provenance

The transfer applies reviewed missing path/hunk differences against the frozen source,
without importing its integration ancestry or replacing the whole tree.

| Original concern | Reconstructed scope |
| --- | --- |
| `af7583b0`, `9cba234d`, `e273a0d9`, `dc8942b4`, `b758695c` | Deferred Curve probe and driver, curved-wall/hosted-opening continuation, native Fit, rotating all-edge measurement proof and explicit frame Promise body. |
| `8b543b7f` | Stair/Arrow native-input driver, read-only element probe and fidelity accessor. |
| `1856831b`, `ecfbb2a4`, `38c821fc` | Supplemental Reference/Photo/Opening placement matrix, opt-in search catalogue, frame executor and scoped script/test cleanup. |
| `b6d6d581` | Shared native Add-menu keyboard helper and the Curve/Opening driver callers; production cleanup is already in the quality parent. |
| `c2215a79`, `8194eeda` | Real idle hover before arrow assertions, exact vertex counts, M00 state after deletion, named Inspector controls and current layer checkbox. |
| `2c872609`, `408c7dea`, `46f30207` | Remaining rotation admission/lifetime cases, deferred read rejection and compensation-aware failed-write history proof. The previously extracted curve-only case is merged into the original kind/curve matrix once, not duplicated. |
| `eb59fc2b`, `38c821fc` | Preserve both Group release/history and Escape cases while separating their shared setup; no Group command/census test is duplicated. |
| `c920921c` | Await initial reference-preview paint before counting theme redraws. |
| `20488a00` and frozen follow-ups | Expanded user guide, task/spec continuation, current gate links and historical source-specific receipts. |

The frozen test order and icon-fixture key order are retained. `.fallowrc.json` gains
the three actual supplemental executable entries and matches the frozen catalogue;
its obsolete standalone subject-kind entry is removed without deleting or modifying
that script. Thresholds/exemptions are unchanged.

## Exact static comparison

At the reconstructed commit, `git diff b10c3b24 -- tests scripts .fallowrc.json`
is empty. There are **no remaining test/script/harness/config differences** in those
paths. The quality parent's `src` and `styles` remain byte-identical to `b10c3b24`.
The original final nine-journey runner, 18-comparison generator and original visual
journey scripts are unchanged by this branch.

The complete implementation evidence directory is also byte-identical to the frozen
source, including binary images, failed reports/logs and their original source SHAs.
The partial `a79baa83` audit remains explicitly failed/partial, not promoted to final
acceptance. Existing delivery receipts absent from the integration tree are retained
as intentional documentation differences; none was deleted for tree equality.

## Current verification boundary

The coordinator reports that frozen `b10c3b24` passed all 730 test files / 8,624 tests.
Its complete check still fails the unchanged coverage gate: 97.28% branches, 98.79%
statements and 98.98% functions. New post-b10 coverage additions are under separate
combined verification and are intentionally excluded here. Historical receipts below
retain their dated results rather than being rewritten to imply later validation.

Pending work remains the unchanged combined quality gate, all nine final journeys and
18 actual image comparisons, the supplemental feature matrices, inspection of their
actual images, applicable isolated Obsidian acceptance, and reviewable publication.
No native/device/screen-reader or final visual acceptance is claimed by reconstruction.
