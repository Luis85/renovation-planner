# Trade, schedule and quote finalization evidence

Status: bounded implementation checkpoint after integration `2c2c1d71`. The source and new tests below have targeted verification; the final combined gates have not passed. [ADR-0024](../../../../development/adrs/0024-trades-manual-schedules-and-quote-comparison.md) records the accepted source contracts and implementation decisions.

## Saved-source recovery and downstream navigation — 2026-09-07

The integration now remembers the canonical source of the actual successful Work execute/Undo/Redo operation and the ID of a newly saved Quote. After a successful write with failed read-back, Open source note resolves that ID through the existing Project index/host note seam. Work history across two floors opens the floor of the actual history operation; Quote recovery works even when the retained comparison contains no offer. The action coalesces pending opens, is inert during read-back, reports a missing source without recreating it, and leaves already reported host faults as a single notice. It does not replay writes or add a persistence authority.

Retry in the Work and Quote views uses the integrated downstream focus helper: if the successful read removes the focused warning control, focus returns to the persistent Project Back button. The recovery task's Edit/Revise/Work dialog captures remain intact. English and German missing-source copy is generic to the actual source record.

Five native RED cases reproduced four missing-source actions and Retry losing focus on c1091086. The corrected joined run passes **38/38 tests in eight files, 34.31 seconds**, including seven saved-source cases, five navigation/partial-data cases, three lifecycle cases and existing Work/Quote/dialog-focus regressions. Initial expanded test probes were corrected to observe actual openFile for a new note, schema-refused valid YAML, and completion of the entire canonical Work command (Plan plus geometry), preserving the assertions on identities and bytes. Native paused drafts retain invalid raw date input through source opening and read retry; a landed command finishing after disposal performs no late read-back or notification. No threshold, timeout or exclusion changed.

This is browser-environment/FakeVault evidence. Public ItemView return checks and joined UI choice controls pass 20/20 tests in five files, 25.11 seconds; five navigation cases overlap the 38-case run, for 53 distinct cases across both runs. Final types, whole Oxlint, scoped ESLint and diff checks pass. Full coverage, final nine browser journeys/eighteen references and real Obsidian acceptance remain separate verification requirements.

## Complete c1091086 verification — 2026-09-07

Exact source c10910868f4da7f5a246dbed44b129db6e6f5a4d stayed unchanged through the local run (tracked tree be4d39b9a6e20564565ccf0d283144e73ba01bf2). Build/types and whole Oxlint/ESLint passed. All four CI legs pass all 625 files and 7,967 tests, with 69 skips. The local two-worker run passes 622 files/7,761 tests but fails two 60-second lint hooks (layer-boundaries and the vue-rules dialogs boundary) and one 60-second lint-edited test; 274 tests are skipped after those hook failures. Test/coverage time is 1285.98 seconds; complete check time is 1389.95 seconds. No timeout changes were made.

Local and every CI leg have identical full coverage: 98.68% statements (17687/17923), 96.87% branches (12221/12615), 98.65% functions (5053/5122), 99.31% lines (13868/13964). Three unchanged floors fail. JSON and lcov were preserved before any scoped run. The separate complete Fallow run against this fresh coverage passes: zero dead-code issues, zero reported clone groups and zero above-threshold health findings. The existing reviewed clone exclusion is unchanged. This settles the four previous coverage-dependent CRAP findings; it does not settle the remaining coverage floors.

A subsequent source audit identified reachable downstream gaps: focus return when an edited Quote becomes received or a peer removes a Work/Quote row during its dialog, and relevant Markdown source access after saved/read-back-failed operations. Recovery owns the dialog focus correction; root owns saved source identity and the read-only source action, including Work Undo/Redo across floors and focus after Retry removes its control. Native regressions are prepared; these corrections and their final browser acceptance are pending. Fresh uncovered locations are being used for bounded native verification; no unfinished requirement is deferred.

## Production paths

Shared Trades and Suppliers use named-note repositories, the configured Library, the Project Index and existing guarded catalogue services. Library migration now includes both within its source-folder intersection. Work retains its Plan authority, shared identities, existing RenovationForm/command/history and explicit date endpoints. Project Work reads each floor register and displays Room context and existing dependency blockers.

Project quote notes use the shared conditional note writer. Expected versions are checked again inside the host frontmatter callback before mutation. The same fresh callback content enforces received immutability and project ownership. Stable creation retries compare canonical quote facts; property insertion order cannot create a false conflict. Offer lines keep exact decimal Money and explicit floor-qualified Work/catalogue scope. The separate Project table aligns equal scope sets, exposes missing scope and totals each offer/currency independently.

Project destination/origin state uses the existing host navigation. Contextual editor arrival is queued per physical leaf after a Plan-only reveal, so a plain open and a contextual open cannot create two editors for one floor. The existing editor applies selection/focus after hydration and guards save, dialog and active-draft state.

## Integrated correction findings — 2026-09-07

The full cd362dd0 run failed: local 7,903 passed/7 failed/70 skipped, CI 7,906 passed/5 failed/69 skipped. Both found the note-contract helper outside the harness import roots, service census and openPlan assertion changes, missing application error translations and a read-paused element draft incorrectly frozen. The two local-only lint cases exceeded existing timeouts; those limits remain unchanged. Fallow separately reported four unused-export/public-type findings, one repeated structure recovery block and seven complexity/coverage findings. These are correction work, not an accepted gate.

A controlled two-file run passed the same 15 cases with either environment. Two newly added tests imported the browser plugin composition under Node, loading an additional server template transform. The explicit per-file jsdom setting used by existing composition tests restored unchanged MaterialFields from 71 branches/21 functions to 34/18, and CostFields from 63/29 to 28/25, matching the prior full artifact. A CLI-only environment override did not change the project transform. This corrects the test environment; it does not exclude source or alter coverage thresholds. Partial-run global coverage deliberately still fails the full gates.

Three real command/native Quote view regressions reproduced stale Asset rename/deletion and Room rename labels before the correction. Quote projections now reuse the canonical Asset change source and subscribe to the relevant Work/Room/offer events. Matching index rebuild and Asset events are delivered once. Draft choices retain raw text and selected identities while their labels refresh. All three Quote cases passed after the event correction. The seven-file correction run passed 156/158 cases: harness import, element read-pause recovery and contextual openPlan contracts passed; an unrelated missing project-not-found translation and deferred-rejection test fixture remained and were corrected afterward. The corrected production build/types pass. Whole-tree Oxlint/ESLint found only seven template formatting warnings; scoped formatting and lint then passed. The expanded 17-file run passed 273/274 and exposed stale Project Work rows after a correctly refused peer-conflicted Undo. A read-only refresh now updates those rows, with all eight Work flow/recovery cases passing afterward. Disabled history actions also check history availability before dispatch; the extended Work suite passes all nine cases. The two local-only lint timeout files passed all 44 cases in a bounded recheck without changing their timeouts. Fallow reports zero dead-code issues; its Work template finding prompted a separate stateless controls extraction. The old full coverage still reports untested downstream action complexity, so only a fresh full run can settle that metric. The final full gate remains pending.

## Joined 9e2b89f8 verification and remaining corrections

All four CI legs (Linux Node 22, 24 and 26; Windows Node 22; run 34104510658) agree: 623 files pass and one fails; 7,946 tests pass, one fails and 69 are skipped. Build/types and whole lint pass. Coverage is 98.61% statements (17674/17922), 96.78% branches (12206/12611), 98.65% functions (5050/5119) and 99.31% lines (13866/13962). The unchanged statement, branch and function floors fail; analysis is not reached. The separate dependency audit passes.

The remaining failing test correctly rejects jsdom in tests/application/schedule/projectWork.test.ts. The browser plugin command cases are separated into tests/plugin/projectWork.test.ts; the pure Project Work query stays under the protected application directory, uses the repository fixture without importing the plugin composition and executes in Node. The guard, environment configuration and thresholds remain unchanged. This supersedes the earlier claim that the per-file environment correction alone was sufficient.

The final browser runner on UI 0f0fdab1, identical whole tree to root 9e2b89f8, passed the first planning and renovation matrices (four scenarios each), then stopped at Reference reflow focus. Escape closed the dialog, but narrowing from 1440 to 460 left focus outside the editor because the original action remained connected inside a hidden region. Recovery correction 00852947 is integrated as 66438ff3; the existing browser assertion remains unchanged. This is a failed final capture, not nine-journey or eighteen-reference acceptance. UI 67cffc84 (integrated as 803039ae) also makes the final Work/Costs image sources use native Project navigation and adds persisted planned/committed/actual costs to the quote journey. That capture change has syntax checks only until the next browser run.

The bounded boundary/refusal run passed the unchanged environment guard, both plugin integration cases and all sixteen Quote repository cases. Three new Node query byte assertions initially compared the FakeVault Map subclass with a plain Map and failed despite equal contents; they now compare every persisted path/byte entry. The corrected Node query and joined Reference/helper run passes all 48 cases in 21.69 seconds. No production change was needed for those query assertions. Joined vue-tsc, whole Oxlint and scoped ESLint pass, including the final inline focus-helper signatures. The full gate remains pending.

## Verification record

- Node 24.20.0, two workers: the initial downstream run passed 76/79 cases. Two failures were ineffective fixture insertions; the third exposed a stable-quote retry comparison defect. Parsed-frontmatter fixture assertions and canonical quote-fact comparison corrected them. The expanded 15-file run passed all 134 cases in 40.87 seconds.
- Broader repository/navigation/native UI verification after lint refactors and UI integration: 49 files, 980 passed/2 failed, 162.42 seconds. Both failures were existing completeness guards requiring the three new note kinds in shared refusal/identity/preservation tables. Those tables now exercise real Trade, Supplier and Quote repositories through the unchanged contracts.
- A follow-up run passed all 76 cases in those two repository files and the generic-element interaction file, 12.72 seconds. New Edit/Delete regressions first reproduced peer-deleted elements remaining displayed (11 passed/2 failed); projection comparison now occurs before missing-element return and both pass, including selection/Inspector cleanup and no writes.
- Native catalogue disposal regression first failed with one unwanted repository save after dialog unmount (1 passed/1 failed). The form now refuses late submission. Both disposal/authorized-save completion cases passed in the broader run.
- The broader run includes atomic quote peer title/status/revision changes inside the host callback; native Supplier/Quote creation and received revisions; explicit scope gaps; retained raw drafts and updated choices; successful-write/failed-read retry; conflict preservation; expiry timer cleanup; per-leaf arrival ordering and actual editor returns; retained Project reads; Trade rename/missing identity; Library relocation; and ten source lifecycle/marker regressions from hardening.
- After the projection comparison extraction, all 53 cases in five element/Room/naming/outline/retry files passed in 88.38 seconds.
- Production build/types passed. Full Oxlint/ESLint passed with zero warnings before the final disposal/projection/test corrections; subsequent whole Oxlint and affected ESLint also pass, including the extracted projection comparison within the unchanged complexity limit. Earlier 483 focused spatial/read-only cases remain historical evidence.
- Source-only recovery review found missing Trade/date comparison fields and named catalogues in Library relocation, both corrected by root. It traced remaining Work copy, form, shared-link and compensation paths without an additional finding. This is review evidence, not host acceptance.

The unchanged full coverage/quality gates, final eight browser journeys plus the downstream ninth journey/eighteen image comparisons, performance/cleanup measurements and required live-host observations remain open. No timing or browser claim is transferred from an earlier source tree.


## Post-restart native boundary verification — 2026-09-07

Production remains unchanged from `5557385f`. The local continuation `a7d61da1` also incorporates E `7fd89801`. The seven-file root run passed 26 of 29 cases in 64.79 seconds. After correcting fixture assumptions, the two affected files passed all seven cases in 13.98 seconds. Together these runs establish 29 distinct passing cases, including the eight newly joined arrival/deletion cases. Root adds 14 cases across `downstreamSavedSource`, `projectSourceNavigation`, `downstreamUnavailableSettings`, `quoteIsolation` and `assetDefinitionPeerChanges`. They exercise native Vue controls, canonical composed repositories and the host note-opening seam.

The three initial failures were fixture assumptions: completed Project rows follow their actual displayed order; hand-edited Quote frontmatter must reach the fake host MetadataCache before a metadata-backed read can observe it. The corrected tests retain their behavioral assertions and observe those boundaries. Source review also replaced a nonexistent FakeVault method and retained signed quote credits allowed by Money/Quote. Typed Project identity, typed navigation mocks and immutable sorting satisfy existing lint rules. Final whole Oxlint and scoped ESLint pass. Type checking passed before the final mock/sort-only corrections and remains part of the next joined build. No new production change or full-gate pass is claimed.

CI on `5557385f` passes 633 files and 8,001 tests with 69 skips, production build/types and whole lint on all four platforms. Coverage fails statements at 98.86% and branches at 97.27%; functions at 99.02% and lines at 99.43% pass. Audit passes. Logs are preserved in the finalization scratch directory as `root-resume-native.log`, `root-resume-fixtures.log`, `root-resume-types.log`, `root-resume-oxlint.log` and `root-resume-eslint.log`.
