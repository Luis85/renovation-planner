# Contextual detail layout

This continuation addresses structural differences between the locked M08–M14 references and the joined production editor at `24ee3177fa3d088cd52b05430ae33a972428494d`. Final rendered acceptance and combined verification remain pending.

- M12 uses native tables grouped by Work, with named column and row headers, stable numbered material identities, comparable Needed/Purchased quantities and visible Calculated/Manual qualification. A labelled, keyboard-operated disclosure retains the existing calculation, reserved/outstanding amounts, procurement and record actions. No quantity or procurement formula changes.
- M13 groups the existing scoped cost rows by Work, with an explicit Unassigned fallback. Group planned totals reuse the same refusal-aware money reconciliation as the overall total. Following a record or linked material estimate reveals its group before native focus returns to the record.
- M08–M14 keep one mounted contextual navigation instance. Detail modes expose it through a compact labelled button; Overview retains its three main routes and linked summaries. Selecting a destination closes the disclosure and returns focus to the visible context button, or Details when returning to Overview.
- M14 phase filters are native buttons with pressed state. The numbered photo gallery reuses existing vault previews and shows selected metadata below; empty selection remains unselected. Metadata-only previews do not create duplicate images. Documents and Notes retain the contextual list. These presentations retain the existing evidence-phase and spatial-selection state.
- Material and cost creation actions follow their lists and use precise Add labels. German labels and semantic host theme tokens accompany the presentation changes.
- The connected browser journey exposed stale navigation context after selecting a Room in Plan: entering renovation restored the previously inspected wall, causing a Room-area material to be correctly refused for a wall target. Navigation now derives the current target from spatial selection, retaining remembered Room context only when it still belongs to that selected target. The regression exercises Plan → Room selection → renovation entry.

The existing planning journey opens material details using the keyboard before editing allocations and closes them for the M12 comparison. The common navigation helper opens the context disclosure through Tab/Enter before navigating. Materials, Costs and Photos now emit raw axe violation and incomplete-check reports in each of the four browser scenarios; incomplete checks remain manual observations, not automated passes. Object uses the same extracted scan helper.

## Verification

Production build/type check, changed-file ESLint, whole-repository oxlint and `git diff --check` pass. Ten focused files pass all 95 tests. The six-component coverage audit reports 66/66 statements, 81/81 branches, 23/23 functions and 54/54 lines; it is not the whole-repository coverage gate. After the browser exposed the stale Room-entry context, all 57 tests in the five affected navigation/planning files pass with its regression.

Fallow reports no dead-code or duplication findings. The Inspector and Evidence template findings are cleared by extracting the spatial action group and photo gallery. Two pre-existing integration findings in `resolveSelectionTarget` and `ElementShapes` remain owned by finalization; no threshold or exclusion was changed.

The connected planning journey passes light/dark at 1440 × 900, custom accent at 1000 × 900 and German dark at 460 × 900. It uses production commands and repositories over FakeVault, native Tab/Enter/select input, retained drafts during reflow, partial payment, Evidence history and Review navigation. The known binary fixture now uses the same real static PNG resource for reference and gallery rendering, with a decoded-image assertion. The final run is recorded in [evidence](evidence/editor-detail-layout/report.json), alongside representative PNGs and all twelve raw axe reports. Browser: Edge 152.0.4191.62 via explicit `RP_CHROMIUM_EXECUTABLE` override, not pinned Chromium or live Obsidian.

All twelve scans report zero violations. Each has one incomplete contrast category for decorative, aria-hidden symbols; the three narrow German scans additionally contain an unsupported native OKLCH surface. These incomplete results are retained, not counted as automated passes. Visual inspection confirmed readable totals, bounded Material columns, selected photo metadata and the narrow German layout. The first scan found low-contrast material numbers and Add buttons; semantic text and a theme-derived accent mix correct both.

Pending: all other connected journeys, all 18 final full/detail reference comparisons, and the unchanged combined repository gate. This checkpoint does not declare complete visual or release acceptance. Actual Obsidian, screen-reader and physical-device observations remain separately tracked in the completion matrix.

The final capture runner also executes the existing recovery/performance driver sequentially and inventories its fresh output at the same clean source revision. M15 uses the actual successful-write/failed-read-back state, replacing the preliminary synthetic warning fixture for the after comparison. Recovery owns interpretation of that shared run; it is not rerun merely to duplicate evidence. This runner preparation is syntax-checked; final execution remains pending.

## Cost group interaction follow-up

The M13 interaction audit additionally identified that disclosure alone did not highlight the related Work. Explicitly opening an assigned group now follows its real Work identity in Costs and outlines its in-scope spatial targets. Existing record navigation retains Room/surface context, totals, selection and viewport; shared Work highlights each visible linked target without cloning it. Unassigned groups only disclose, and initial/programmatic opening does not change focus. Direct cost/material navigation still expands the exact destination. The connected browser journey now closes and reopens the native summary with Enter, checks focus/current state and unchanged totals, and captures that selected state.

Both focused planning files pass all 27 tests. CostGroup and CostWorkHighlight report 100% coverage: 37/37 statements, 28/28 branches, 13/13 functions and 24/24 lines, with the unchanged thresholds. The regression verifies current geometry precedence, intended-only Object fallback, missing targets, shared context, and hiding outside Costs/Renovate. The existing thumbnail-failure test now dispatches an image error explicitly because the known scan fixture supplies a real resource. This is a focused audit, not the combined repository gate.

Changed-file ESLint, production build/type checking and whole-repository oxlint pass. The four-scenario connected planning browser matrix passes, including native summary close/reopen, focus/current state and stable totals. All twelve axe scans report zero violations; raw incomplete checks remain preserved. The follow-up report, all axe JSON and representative dark/German Costs images are in [cost group evidence](evidence/editor-cost-groups/). Image inspection confirms current Work outlining and readable selected summaries. That inspection prompted an explicit inset summary focus ring; the final visual matrix must recapture this small CSS addition. The preceding browser/build evidence is not claimed to include that later focus-ring declaration.

M15 comparison now targets the recovery driver's `saved-overview` capture: native navigation from the same successful-write/failed-read-back state to Overview, then back to Materials before the read-only retries. That driver update is owned by recovery. The final capture must produce this image before acceptance.

The Work outline uses current target geometry, falling back to intended geometry only for targets added in the planned state; unavailable targets have no invented outline. Work does not own a quantity measurement state. This navigation highlight does not change Material markers' explicit current/intended quantity basis or the existing planned-change overlay.

The final sequential runner also inventories `modal-busy-focus`: recovery's pending-write native-control driver runs once at the same source revision, with its report and renovation/planning pending screenshots copied into the final evidence. This checks focus and guards, not another performance run. Source preparation is syntax-checked; execution depends on integrating verified recovery checkpoint `75084550d5d577943278af44ea99686ddec064e2` and the final joined source.

A subsequent shared-wall audit found that two Room contexts can legitimately reference the same spatial target. Work highlight rendering now deduplicates target IDs after scope filtering, retaining every record relationship. The extended regression covers the adjoining-Room/same-wall case. All three planning marker tests, type checking and changed-file ESLint pass for this follow-up. It adds no persistence or money behavior; the combined gate and final capture remain pending.
