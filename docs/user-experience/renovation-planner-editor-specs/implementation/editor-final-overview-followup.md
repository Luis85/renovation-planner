# Floor overview and wall draft follow-up

The 43041936 comparison identified two remaining presentation gaps. The Floor Inspector now
groups its original planned-change Aggregate and reconciled planning cost in a prominent pair,
keeps counts/areas and their existing partial/unavailable rendering, and shows the existing
selection guidance text. The separate shell live region still owns the once-per-clear
announcement. Room list rows show change counts derived from the existing renovation summary;
stale or incomplete projections show unavailable cues. Area/multiple-selection rows and the
single floating Add route retain their behavior. The information icon uses the already pinned
Lucide fixture revision; production still calls Obsidian setIcon.

Closed wall drafts previously used the same wall body stroke as committed walls. A read-only
overlay in the existing architecture layer adds an accent dashed outline and four-screen-pixel
corner circles on the same draft points. The shared closing point is drawn once. Empty/cancelled
tasks draw nothing, and no geometry, command, validation, persistence or history path changes.

Native verification: the initial six-file batch passed all 120 existing checks, including the
unchanged 96-test focus gate. Two new test fixtures were corrected: Subject records do not
support shared Work/Evidence links, and jsdom needs an explicit accent token. Both corrected
cases then passed (66884, 12.82 seconds), covering actual per-Room facts, partial/stale cues,
draft visibility, constant screen sizing, cancellation and unchanged vault data. Types, whole
Oxlint, scoped ESLint and capture-script syntax passed. Browser verification is still pending.

## Matching-view capture additions

The original 900px screenshots and all original workflow assertions stay in their journeys.
Additional M00/M01/M07 and M13 views use 1000px height with recorded bounds. M07 and M13 use
normal Tab/Control+Home input to show their initial content; no direct scroll, focus or camera
mutation is used. M16 captures the unobscured canvas at 880px in the wide scenarios and at the
original 460px German width, closing and restoring panels through their native controls. Every
supplement checks retained selection, world points, caption text/fonts, camera after restoration
and unchanged vault data. Each has a separate JSON attribution. The nine-journey runner asks
the downstream journey for its additional design view; no existing journey is removed.

The first complete 43041936 run and its comparison inventory remain archived. These new
production/capture changes need their own verified images and subsequent complete matrix before
claiming final visual acceptance. The verified pan stabilization remains intact.

## Capture integrity without deletion

Automatic approval review rejected recursive cleanup of generated captures. The final runner
now retains previous files and records actual successful page.screenshot output paths instead
of scanning PNG folders. A successful journey emits a current-source manifest with original
timestamps and hashes. Copying verifies each explicitly named source file's timestamp and hash
before writing its destination; the nine original reports remain required. Static capture adds
exactly sixteen named states. Every one of the eighteen comparison inputs must occur in that
current manifest, and all thirty-six compositions must be fresh. The final image inventory is
built from those recorded names, never from old failed or unused PNGs. Source/HEAD freeze stays.

A small filesystem check passed fresh named copying, exclusion of an unrecorded fresh PNG,
refusal of changed bytes before copying and refusal of a stale source before copying. Its
temporary test data is retained without deletion. The first pilot's M00 header selector and
the second pilot's ambiguous Floor/Rooms heading selector were corrected without changing
production behavior. Captures now prefer the selected Room and explicitly label any Floor or
element viewport sample, while retaining exact selected-entity IDs separately.
