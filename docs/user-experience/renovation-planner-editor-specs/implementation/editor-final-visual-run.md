# Complete editor browser run at 43041936

The original, unchanged `scripts/editor-visual-final-check.mjs` completed successfully at
`43041936a7f86d31058f6d21a16c3a596e1bacc6`. Terminal session 29535 exited zero. Source and
HEAD stayed unchanged throughout the run, from 2026-09-07 19:35:04 UTC to 19:44:25 UTC.

All nine journeys completed all four scenarios: planning/materials/costs/evidence, renovation,
reference setup, resilience, connected overview, objects, recovery, modal busy focus and
downstream planning. Static state capture and the eighteen reference compositions also
completed. The browser was the explicitly configured Edge 152.0.4191.62, not pinned Chromium
or a live Obsidian leaf. `evidence/editor-visual-fidelity/capture-provenance.json` records
all eleven steps and hashes for 340 original/collated PNGs. The original reports, axe results,
screenshots and terminal log are retained beside that provenance.

## Visual review and remaining verification

All eighteen generated comparisons were inspected. The improved Add catalogue, native creation
choices, reference preparation, record density, material/cost alignment, six-image gallery,
warning hierarchy and issue-rich Review are visible. Different fixture names, record counts,
geometry, amounts and synthetic test-image contents remain explicit; reference content was not
substituted into the product. Existing accepted modal/task and ownership amendments still apply.

The comparisons do not yet establish complete visual acceptance. M00 uses the original
900-pixel-high journey and its continuation falls below that initial view. M07 retains the
Inspector's lower scroll position from its navigation journey, omitting identity/measurements
from the screenshot. M13's expanded cost data approaches the lower crop. M16 compares an open
460-pixel Details drawer with a reference showing the unobscured constrained canvas. Additional
explicitly attributed matching viewport/top/closed-panel views are needed; the original journey
captures and assertions must remain. M01's floor-summary hierarchy and per-Room change cues,
and M04's visible draft geometry over the reference, require closer source/visual assessment.

The recovery performance report is also retained without reinterpretation. Readiness, selection
and Inspector latency budgets passed; twelve close/reopen cycles ended with zero tracked
resources. Pan frame medians were approximately 33.1–33.3 ms, below the 60-fps target. Root is
investigating this separately with a quiet diagnostic. Driver exit zero does not establish a
performance pass. Full coverage, live-host and final product acceptance remain separate gates.
