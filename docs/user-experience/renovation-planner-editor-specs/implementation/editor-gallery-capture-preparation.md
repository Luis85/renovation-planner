# WIP — representative M14 gallery capture

This is source preparation, **not verified gallery or final visual acceptance**. It depends on
the finalization branch's new Evidence date and Work-metadata implementation. It must not be
made green on the older UI source by omitting date or relationship assertions.

The design-only variant of the existing planning journey gains six distinctly labeled synthetic
PNG inputs, using the established browser canvas/PNG fixture technique. They are ordinary
test-vault file inputs resolved through the existing reference-workspace resource mapping.
No Evidence records or editor stores are pre-populated, and no photographs are extracted from
the locked mockup.

The driver creates six relationships through the native production forms, with explicit During
phase, dates 26–31 August 2026, existing Floor finish Work and Room-relative pins. Creation order
is reversed so the recorded-date order is observable. It selects the third, Wall opening record,
and checks the 28 August date, Work, distinct identities, six filtered cards and unchanged Room
selection/camera. It captures `photos-gallery` at 1000px height, restores the normal 900px height,
and continues the original Review journey. The ordinary non-design path and all original
assertions remain intact. M14's final comparison selects the new gallery state.

JavaScript syntax and `git diff --check` passed. No TypeScript, browser, accessibility or layout
pass is claimed for this preparation. A whole Oxlint attempt caught a shadowed fixture variable;
that spelling was corrected source-only and still needs the next lint run. The identical
Promise-executor block-body correction already applied by finalization is retained here.

After integration, assess actual gallery proportions, visible date/Work metadata, selected-state
association and primary-action hierarchy against M14. A six-image count alone is not fidelity
evidence. All eighteen screens still need current matching-state visual review; the previous
complete runner stopped after six successful journeys in Recovery.
