# Group and rotation admission coverage — 2026-09-09

The complete single-worker run at `b10c3b24` passed all 730 files and 8,624 tests (70 skipped), build and lint. Coverage remained below the unchanged statement, branch and function floors; a separate Fallow run using its fresh coverage passed.

These proposed cases exercise long Room names when grouping, real transient member expansion, previously offered Group/Ungroup actions blocked by a numeric dialog, enclosure crossing an existing wall, Object-bearing groups in Renovate, invalid/full turns, selection retirement during connected-wall review, and numeric rotation retry/source-note/conflict/disposal paths. They use production runtime and repositories over the synthetic vault. No production code or threshold is changed.

Source is committed for one combined focused verification batch. Only `git diff --check` has run at this checkpoint; no test or coverage gain is claimed yet.
