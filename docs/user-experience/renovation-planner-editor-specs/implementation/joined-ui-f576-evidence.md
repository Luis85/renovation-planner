# Joined editor UI candidate

Root merges UI commit f576d13c into the integration branch after Root1b554540.
The merge is conflict-free. This preserves the shared retained Evidence projection,
phase-preserving navigation and independent Work links, and adds the UI presentation,
measured dimension-caption obstacles, shared Review Room/list projection and
contract-correct marker/issue split.

Owner evidence:45/45native tests across5files,66.38seconds; current types, whole
Oxlint and scoped ESLint pass. Caption verification includes meaningful predecessor
REDs, native/lifecycle GREEN and actual browser pan/inline/clamp observations.
The older four-scenario captures and subsequent corrections remain versioned in
the included owner evidence; they do not establish final acceptance for this join.

The joined tree now needs its own full CI/gates and desired Review regressions,
then the original9journey/18reference runner, actual image review, performance
and host acceptance. No GitHub PR was merged and no final pass is claimed.

Coverage-owned pending Review tests remain in PR92 until their intended behavior
is verified on the matching UI source. UI-owned historical tests were updated to
the accepted one-marker-per-Room/StayReview contract while keeping explicit issue
source navigation and non-vacuous marker assertions.
