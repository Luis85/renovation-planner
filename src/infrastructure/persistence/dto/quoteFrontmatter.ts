import { z } from 'zod';
export const QuoteFrontmatterSchemaV1 = z.object({ type: z.literal('renovation-quote'), 'schema-version': z.literal(1), id: z.string(), revision: z.number().int().nonnegative().catch(0), project: z.string(), supplier: z.string(), title: z.string(), issuedOn: z.string(), validUntil: z.string().optional(), status: z.enum(['draft', 'received']), items: z.array(z.object({ id: z.string(), description: z.string(), amount: z.object({ amount: z.string(), currency: z.string() }), assetIds: z.array(z.string()), work: z.array(z.object({ planId: z.string(), workId: z.string() })) })) });
