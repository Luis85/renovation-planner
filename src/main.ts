/**
 * The bundle's entry, and nothing else. Obsidian loads the default export; the plugin
 * class and everything it composes live in `plugin/`.
 */
export { default } from './plugin/RenovationPlannerPlugin';
