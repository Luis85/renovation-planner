/**
 * What the asset designer says about selecting SEVERAL parts by dragging a box (AD08's
 * remainder): the marquee itself and the count it lands on.
 *
 * Its own module for `assetEntryPaths.ts`'s reason. Spread into `en/editor.ts`.
 */
export const assetMarqueeEn = {} as const;
