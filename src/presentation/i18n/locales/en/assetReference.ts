/**
 * Wave 3 vocabulary, scaffolded empty so two cards can be authored at once without sharing a
 * table — the integration hazard AD01 §2 names. Deleted at integration if its card adds no
 * strings, the way the marquee's pair was. Spread into `en/editor.ts`.
 */
export const assetReferenceEn = {} as const;
