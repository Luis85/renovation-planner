/**
 * Copy for the clearance helper's link control, Advanced fold and Show clearance toggle (AD18-R17). Created empty by the integrator so that each task in the
 * second parity round owns one locale module and no two tasks edit the same file.
 */
export const designerClearanceHelperEn = {} as const;
