---
type: "renovation-zone"
schema-version: 1
id: "pantry"
revision: 1
project: "proj-unreadable"
plan: "plan-ground"
name: "Pantry"
zone-type: "room"
status: "planned"
---

The zone each case CORRUPTS, one way per case — a future `schema-version`, a malformed one, or
a field the mapper refuses. It is checked in HEALTHY so that the corruption is performed by the
test that depends on it and is visible where it is asserted, rather than being a property of
this folder that every later reader has to discover.
